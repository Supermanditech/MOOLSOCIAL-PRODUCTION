# Privileged functions

Cloud Functions 2nd gen TypeScript code lives here when a journey first needs a
privileged workflow. Basic user-owned CRUD remains generated by SQL Connect.

Functions are reserved for payment/provider webhooks, authoritative stock
reservation, fraud-sensitive commands, scheduled expiry and asynchronous work.
