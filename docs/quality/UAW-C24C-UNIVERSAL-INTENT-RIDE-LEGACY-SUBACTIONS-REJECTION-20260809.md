# C24C universal-intent Ride legacy-subactions rejection — 2026-08-09

The production-named Ride case enabled the legacy shell, selected removed
`sub-action-ride-*` controls, and returned through the legacy owner between
vehicle types. That contradicted direct-default and connected-navigation
behavior.

REG656 mounts the real Bike owner and reaches Auto and Cab through the
MoolSocial connected chooser without Home.
