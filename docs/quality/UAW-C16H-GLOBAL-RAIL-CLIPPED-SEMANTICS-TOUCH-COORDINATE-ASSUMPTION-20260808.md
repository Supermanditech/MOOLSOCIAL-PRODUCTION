# UAW C16H global-rail clipped-semantics touch-coordinate assumption — 2026-08-08

## Rejection

After the Buy matrix, a global-rail tap intended for Eat selected Ride/Cab. Fresh UIAutomator evidence reported Ride as current, so no Eat artifact was created or mislabeled.

## Cause and prevention

The global rail's XML semantics are vertically clipped to 10 pixels although the visible anchored shell extends below that interval. The retry uses a fresh screenshot to identify the rendered hit region, verifies selected family/local semantics after each event, and only admits a matrix capture when both match. The truthful Ride/Cab state is diagnostic and does not count as Eat evidence.

No build/install/protected state changed; the event was an authorized navigation-only device interaction.
