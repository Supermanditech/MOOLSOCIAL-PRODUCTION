# C25F R11 Chat continuity predecessor navigation rejection

- Date: 2026-08-09
- Status: registered before test migration

The retained R11 Chat continuity suite still opened the removed 164 × 56 destination launcher and then selected Chat from the predecessor connected navigator. C25 intentionally makes Chat a direct one-tap header action and keeps the MoolSocial menu main-domain-only, so Eat, Ride, Book and Work correctly had no `mool-home-launcher`.

The migration changes only the presentation entry control: each origin taps its family header Chat key directly, then retains the exact existing inbox and system-Back return assertions. Social and Shop are added to the same continuity inventory.
