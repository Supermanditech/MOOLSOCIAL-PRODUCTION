# C22E C11 design-token import rejection — 2026-08-08

The migrated C11 reverse-U assertions referenced `MoolLocalNavigationTokens` without importing the shared design-system library, so focused compilation rejected before behavior ran. REG-20260808-542 records the test-only omission.
