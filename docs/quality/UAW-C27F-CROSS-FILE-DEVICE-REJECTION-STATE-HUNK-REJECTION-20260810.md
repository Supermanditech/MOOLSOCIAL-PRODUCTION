# C27F cross-file device-rejection state hunk rejection

The first C27F rejection-state patch coupled scope and APK machine owners and
assumed a successor-preselection evidence value that differed from live JSON.
`apply_patch` rejected the complete mutation. Ticket rejection evidence was
already recorded, while scope and machine state remained at device-matrix-open
with build/install authority closed.

Final rejection state must be applied separately to exact scope and APK owners,
then read back before permanent gates run.
