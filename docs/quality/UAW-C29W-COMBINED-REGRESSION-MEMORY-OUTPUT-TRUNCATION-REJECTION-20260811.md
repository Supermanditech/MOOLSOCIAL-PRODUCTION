# C29W combined regression-memory output truncation rejection

- Date: 2026-08-11
- Phase: implementation
- Result: combined document render rejected

The complete regression-memory document and a registry projection were emitted in one tool result, exceeding the output budget. The partial memory render is not accepted as a complete required read. The retry line-counts and pages the single literal document through verified non-overlapping ranges, then runs the registry projection separately. No product source, build, device or provider mutation occurred from the rejected read.
