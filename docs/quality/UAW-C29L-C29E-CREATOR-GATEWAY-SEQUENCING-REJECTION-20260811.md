# C29L C29E creator-gateway sequencing rejection

After the general protected-test migration, the C29E Social home/dock test correctly found the new creator gateway but still asserted MoolSocial carousel tools before selecting the MoolSocial host. That left one failed case while the other 42 protected cases passed.

The permanent prevention is for C29E to capability-enable and assert both hosting choices, then tap `Create a MoolSocial post` before checking MoolSocial format tools. No build, device, provider or protected runtime changed.
