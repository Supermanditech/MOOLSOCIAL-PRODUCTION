# C30R Play Internal install recovery findings

Date: 2026-08-12

## Founder disclosure

Customer outcome: install the already active C30Q r60.43 release exactly once
from Google Play Internal Testing on the now-package-empty OPPO, prove the
Play-distributed identity and execute the bounded MoolSocial/YouTube reviewer
journeys.

Classification: `mvp_required`. A valid Android reviewer link and truthful
Play/App Check device proof are required for the time-bounded YouTube compliance
response. The release, tester access, AAB, Firebase identity and Dev providers
already exist; recovery is reuse/configuration/test-only acceptance.

The minimum implementation adds no product code, screen, route, backend owner,
provider capability, build or upload. It preserves the r60.40 binary/removal
evidence, verifies package absence, permits one founder-visible Play install,
seals version/installer/Play signing identity, then runs the previously bounded
device matrix and exactly six Dev Create writes after a real Feed read.

Excluded: r60.40 reinstall or local-data recovery claim, ADB sideload, another
build/upload/install, Production/open/public track, Staging/Production backend,
YouTube upload or scope expansion, secret/private verdict/nonce access, email,
quota submission, commit/push/merge/main, messages/calls/funds.

Dependencies: active Internal release `2026081243`, eligible founder tester,
Play App Signing SHA-256 registered in the exact Firebase Android app, preserved
Dev provider revisions and C30K corpus, connected OPPO, and founder handling of
any visible account/password/MFA/device confirmation.

Test plan: package-absence gate; one Play install; package/version/installer/
signer and split identity; C28D; full YouTube reviewer matrix; real Feed;
exactly six Create/readbacks; interaction/resilience/navigation; unsent reviewer
package and final founder approval hold.

## Reconciled predecessor state

The founder removed debug-signed r60.40 after Google Play rejected its signer
transition. Read-only ADB proved `com.moolsocial.app` absent; Codex did not
perform the uninstall. The old APK remains preserved, but its former private
local data is treated as removed and non-restorable from the identity record.
