# Buy MVP ready-order-to-delivery portfolio authorization

Date: 5 August 2026
Branch: `remediation/prototype-conformance-2026-07-20`
Predecessor HEAD: `f6dfe7587aa02d782e94282d14af8bafff48ded0`
Portfolio: `BUY-MVP-READY-ORDER-DELIVERY-PORTFOLIO-20260805`
Machine-manifest SHA-256:
`5CE81AB6332607A71B960C57A1E99466109E13299CD8A5995FFC3163F35893AF`

## Founder authorization evidence

After receiving the complete tap-to-tap customer, provider, Admin, delivery and
recovery journey, the founder stated:

> preauthorised to implement execute and oppo testing

The statement records advance authorization for sequential implementation,
execution, machine-gated builds and checksum-matched OPPO testing for the exact
47-ticket manifest above when execution is later activated and each ticket's
listed dependencies and mandatory gates pass. It does not itself activate a
ticket or start implementation. It does not
authorize commit, push, deploy, promotion, Production writes, credentials,
funds movement, live payment, live WhatsApp, live telephony or any waived
finance, legal, tax, clinical, privacy, security, provider, environment,
accepted-reference, APK-machine or release gate.

Only one logical portfolio ticket may be active at a time. Ticket-specific
machine authority must be narrowed to the operations that ticket needs.

## First candidate disclosure — not activated

Ticket: `BUY-MVP-ADMIN-ACCEPTANCE-SLA-POLICY`
Classification: `mvp_required`

Customer outcome: an authorized Admin can control bounded provider-acceptance
windows, sequential-attempt limits and an overall assignment ceiling instead
of relying on hard-coded or indefinite timing.

Classification reason: trustworthy reassignment cannot operate without one
authoritative, bounded and auditable timing policy.

Minimum complete scope:

- a server-owned, versioned and effective-dated acceptance-policy contract for
  Shop, Wholesale, non-prescription Medicine and prescription Medicine after
  pharmacist-ready review;
- validation for 30–300-second per-partner windows, one-to-five sequential
  partner attempts and a coherent overall assignment ceiling;
- authorized create/revise commands whose changes affect future orders only;
  and
- immutable policy identity, revision and audit facts suitable for later order
  snapshotting.

Explicit exclusions:

- automatic recommendation or automatic Production policy mutation;
- mutation of an active order's snapshotted policy;
- policy-history review, rollback and maker-checker controls owned by
  `BUY-MVP-ACCEPTANCE-POLICY-AUDIT-ROLLBACK`;
- provider selection, order assignment, payment, inventory or delivery logic;
- Flutter or HTML presentation work; and
- live WhatsApp, telephony, payment, credential or other external-service use.

Dependencies and approvals:

- the exact authorized portfolio manifest above;
- existing Admin authorization ownership; and
- the active MVP scope gate. No external provider credential is required.

Test and evidence plan:

- valid and invalid window, attempt and ceiling boundaries;
- Admin authorization and cross-tenant denial;
- deterministic version/effective-date and future-order behavior;
- concurrent revision, idempotency, immutable audit and rollback behavior;
- focused domain tests plus two affected regression passes; and
- predecessor/source manifest and final source-drift evidence.
