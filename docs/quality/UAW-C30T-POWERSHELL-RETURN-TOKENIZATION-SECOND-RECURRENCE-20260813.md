# C30T PowerShell `return` tokenization second recurrence

- Date: 2026-08-13
- Device: OPPO CPH2375, serial `2b3e0f71`
- Installed artifact: Google Play Internal Testing `1.0.0-r60.44 (2026081244)`
- Scope: navigation-only comprehensive journey inventory

After Shop, Food, Travel, Care and Work had each passed exact semantic navigation and evidence capture, the first helper intended to reopen the main menu contained `return$d` instead of `return $d`. With `ErrorActionPreference=Stop`, the initial hierarchy helper terminated before any tap. The OPPO remains on Work and no application, content, backend or distribution state changed.

The recovery must use expanded multiline PowerShell only, with one statement per line and visible whitespace after every language keyword. It must freshly prove MoolSocial focus, tap the exact main-menu node, recapture the menu, then tap the exact Social node. No compressed inline helper is permitted.
