# C17G r60.18 OPPO build-recovery preselection

State: `selected_prebuild_pending_build_install_closed`
Date: 2026-08-08

## Customer outcome and MVP classification

C17G provides one new checksum-identical OPPO candidate for founder review after C17F produced no candidate APK. It is `mvp_required`: the six-family clear-glass correction is host-qualified but cannot reach founder acceptance until one real installed successor proves Social, Buy, Eat, Ride, Book and Work on OPPO.

## Reuse and duplicate disposition

The ticket reuses the unchanged C18D 1,182-test source seal, the six-family native-Flutter owners, approved UI locks, existing routes/state/copy/content, the single-build wrapper, APK machine gate and in-place installation workflow. It adds no screen, route, feature, backend, state owner or subaction. Bounded inventory found no r60.18 version, `2026080818` version code, C17G-r60.18 artifact directory or candidate collision.

## Minimum complete scope

Re-seal the final source and dirty inventory; prove the r60.16 installed predecessor; pass scope, memory, approved-UI, brand, copy and APK prebuild gates; run the authorization-negative self-test; open one new r60.18 build; execute it once in a persistent cell; validate package, version, signer, checksum and postbuild source identity; install once with `adb install -r`; prove installed checksum identity; then capture the complete six-family main/selected-state, motion, Back, Mool, Chat and content-reachability matrix.

## Stop boundary

Any source drift, protected gate failure, ambiguous build process/output, signer mismatch, package/version mismatch, checksum mismatch, device lock/disconnect, install failure or need for runtime mutation stops C17G. No second C17G build or install, uninstall, data clear, downgrade, protected reference change or promotion is allowed. Founder device acceptance remains pending.
