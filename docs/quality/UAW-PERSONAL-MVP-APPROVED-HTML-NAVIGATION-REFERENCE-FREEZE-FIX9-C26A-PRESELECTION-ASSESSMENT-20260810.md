# C26A preselection — approved HTML navigation reference freeze

Classification: `mvp_required`.

Customer outcome: the exact founder-approved embedded switcher and bottom
action design become durable immutable authority before Flutter replacement.

## Reuse and duplicate search

- Reuse the existing temporary HTML as the sole source file.
- Reuse the additive approved-reference manifest/version workflow.
- Create no runtime screen, route, controller, backend owner or customer state.
- A duplicate search found no accepted navigation reference that freezes this
  exact C26 switcher/rail contract; existing Screen 04 and Buy references remain
  protected and unchanged.

One immutable reference package and one machine-readable interaction contract
are necessary because `tmp/` is not durable acceptance authority. Timeline
impact is under one day and remains within the founder's 60–75-day lock.

## Boundaries and evidence

Only the three founder-approved navigation points are frozen. The temporary
review page's illustrative product/service content is not production content
authority and is not copied into Flutter. Acceptance requires exact source and
package checksums, additive manifest registration, valid JSON, and unchanged
Screens 01–03 locks.
