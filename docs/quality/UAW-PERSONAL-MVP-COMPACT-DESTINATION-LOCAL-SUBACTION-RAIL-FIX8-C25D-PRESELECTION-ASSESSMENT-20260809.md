# C25D preselection assessment

C25D reuses `MoolDestinationNavigationV2`, its already supplied `localNavigation`, `MoolLocalNavigationRail`, design tokens and the C25C menu opener. The rejected r60.23 composition ignored the local rail and rendered a 164 × 56 brand capsule inside a 64 px destination stripe. The smallest correction is one 60 px transparent Scaffold-owned rail: previous, the direct local action cluster, a compact MoolSocial opener and next.

The local cluster adapts between two, three and four actions without horizontal scrolling or sparse full-width distribution. At 320 px, four actions retain 44 px targets; at wider sizes labels gain width. The Scaffold bottom-navigation slot reserves the rail, so it covers no product grid or hit target. No screen, route, backend, service or state owner is created. Build/install/external authority remains closed. Timeline impact is one day within the delivery lock.
