# C07 durable Mool home host completion

Date: 7 August 2026
Ticket: `UAW-PERSONAL-MVP-GLOBAL-MOOL-BOTTOM-RAIL-NAVIGATION-FIX1-C07-DURABLE-MOOL-HOME-PERSISTENT-ROOT-RAIL`
State: `HOST_QUALIFIED_DEVICE_NOT_RUN_BUILD_NOT_AUTHORIZED`

## Implemented outcome

- `/app/mool` remains the existing production route and
  `PersonalMoolRootV2` remains its owner.
- The founder-rejected question-and-grid launcher copy and structure are
  removed.
- The Mool body is now a durable home that displays only truthful local context:
  the selected area and, when an origin exists, `Continue <origin>` using the
  same exact Back callback.
- Social, Buy, Eat, Ride, Book and Work now live in the persistent Mool-home
  middle rail between selected Mool and Chat. The rail scrolls horizontally at
  readable 44+ point targets rather than compressing or opening a popup.
- Focused main-action owners retain only their approved sub-actions between Mool
  and Chat. Existing route, stack, session and lifecycle owners are reused.
- Mool retap is idempotent. Header Back, Android Back and Continue share the
  exact origin-return policy.
- Explicit semantics no longer merge visible child text, and compact 320×568 at
  140% text has no dock overflow.

## Permanent regression practice

The ordered U01-U22 scenario ledger is now machine-required. The static gate
rejects Social alias, popup/modal/ribbon, the full-screen question/grid launcher,
navigation-only intermediaries and missing durable-home/root-rail markers.
Regression memory contains the repeated founder-directive failure and every
development/test command mistake observed during C07.

## Host evidence

- Complete Universal: 183 passed × 2.
- Complete Buy: 360 passed plus 20 established skips × 2.
- Protected Social runtime owner: 13 passed × 2.
- Connected journeys: 99 passed × 2.
- Qualified non-golden mobile: 117 files, 20/20 zero-exit batches × 2.
- Format: 391 files, zero changes.
- Full Flutter analysis: no issues.
- Positive repository gates: 13/13 zero exit.
- Navigation contract: U01-U22, durable home and persistent root rail passed.

The accidental protected golden run and its diagnostics are preserved; no
golden was updated. Approved UI/Social/Buy baseline gates retain their same
pre-existing expected rejection and remain separate release blockers.

## Device and release disposition

No C07 APK was built or installed. OPPO still contains r60.7 and remains parked
on the rejected Mool launcher for founder-visible evidence. C07 is not described
as fixed on device or production-qualified. A fresh unique candidate, source
seal, complete APK machine gates and in-place-only OPPO upgrade are required
before U01-U22 may be replayed.
