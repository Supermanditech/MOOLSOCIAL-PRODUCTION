# C24F legacy Buy compact content-key migration rejection — 2026-08-09

The first migrated compact test retained its isolated legacy delivery fixture
but attempted to measure Buy V2 search, scanner and category owners. The first
V2 key was absent. The correction preserves the fixture's real basket,
address and pickup controls and changes only the shared navigation assertion:
one connected `MoolSocial` launcher and no rejected local rail.
