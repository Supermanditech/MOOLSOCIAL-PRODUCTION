# REG2698 — C34G focused gate is active-ticket-bound

## Outcome

The first C34H source gate failed closed when it directly invoked the qualified C34G focused loop-control checker. That checker requires C34G to remain selected. No source seal, full test, build, Play or OPPO action started.

## Prevention

Rejected C34G remains unchanged. One thin C34H successor-replay checker binds the current C34H scope and immutable C34G repair, then executes the same focused behavior in both PowerShell hosts before any seal.
