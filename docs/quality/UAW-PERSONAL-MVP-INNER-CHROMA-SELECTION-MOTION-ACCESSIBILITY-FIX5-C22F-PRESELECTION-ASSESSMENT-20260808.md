# C22F internal chroma selection, motion and accessibility preselection — 2026-08-08

## Customer outcome

Every Social, Buy, Eat, Ride, Book and Work main/subaction capsule keeps the same neutral smoked-glass base. Only press or selection reveals a restrained radial chroma source clipped inside the medicine capsule, so background and text remain legible and no family theme becomes a strip or panel.

## Reuse and token selection

C22F reuses `MoolLocalNavigationTokens`, `_MoolLocalNavigationCellState` and `_MoolMiddleDockAction`; no alternate control is introduced. Six stable accent tokens are scoped only to internal emission: Social ultraviolet, Buy warm gold, Eat coral, Ride cyan, Book mint and Work blue. Both the selected main action and its subactions resolve the same family token. White icon/text foreground and the neutral base remain identical across families.

The clipped radial core uses one shared geometry and alpha contract. Press is 100 ms; selection and disclosure are 180 ms with immediate zero-duration reduced motion. Selected semantics, restrained optical elevation and the disclosure badge remain non-color-only cues. Every keyed InkWell remains exactly 72×48 (and never below 44 px).

No screen, route, backend/state owner or subaction is added. C22E and all applicable gates passed. Build/install remain closed and installed r60.20 identity remains preserved.
