# UAW C33I Screen 03 passwordless email-link reference successor preselection

State: `VERSIONED_SCREENBOOK_HTML_REFERENCE_PREPARATION_AUTHORIZED_FOUNDER_FINAL_REQUIRED`

The founder explicitly authorized preparation of a versioned Screen 03 successor for Firebase passwordless email-link authentication while preserving Google and Mobile OTP. The prior accepted Screen 03 v2 HTML and native v4 production lock remain immutable.

The ticket is `mvp_required` because the visible numeric Email OTP journey has no backend owner and is a confirmed authentication release blocker. Firebase's supported path changes the interaction contract, so a separate founder-review proposal is necessary before any native implementation.

The smallest complete scope is one standalone local Screen 03 v5 proposal and its local interaction, responsive, accessibility and state evidence. It reuses the existing Screen 03 ownership, setup merge and protected-return contract. It creates no app screen, route or backend owner.

Held boundaries remain exact: no accepted-reference freeze, Flutter/runtime change, Firebase or Hosting mutation, email send, backend deployment, AAB, Play, OPPO, credential, fund, commit, push or promotion. Explicit founder `FINAL` is required before immutable v5 creation or Flutter work.
