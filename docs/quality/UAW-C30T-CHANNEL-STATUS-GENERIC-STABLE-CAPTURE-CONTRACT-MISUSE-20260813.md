# UAW C30T channel-status generic stable-capture contract misuse — 13 August 2026

## Observation

One exact `YouTube channel status` tap succeeded. The next evidence-only step invoked the reusable stable-device helper with its default `Open MoolSocial main menu` requirement. The intended full-page account route has no global rail, so the helper stopped after writing its first readiness hierarchy and before screenshot capture.

## Permanent prevention

- Do not repeat the successful route tap after an evidence-only failure.
- For nested account, search, picker, dialog and provider screens, first inspect a fresh hierarchy and then capture against a semantic proven on that exact route.
- MainActivity foreground identity does not imply the global rail is visible.

## Safety result

The channel-status navigation occurred once. No credential, OAuth value, token, nonce, private verdict, external write, Create write or Chat send was accessed or performed.
