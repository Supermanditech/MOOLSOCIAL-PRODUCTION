# C29P strict TypeScript contract rejection

The first focused typecheck rejected permissive optional-property, indexed-array and Firestore-result assumptions in the new Social backend. The owners were corrected to satisfy the existing strict compiler contract before compiled tests. No endpoint was deployed and no protected runtime changed.
