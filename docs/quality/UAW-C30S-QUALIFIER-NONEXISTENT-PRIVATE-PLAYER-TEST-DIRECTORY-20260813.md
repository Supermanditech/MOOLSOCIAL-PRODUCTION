# C30S qualifier nonexistent private-player test directory

Date: 2026-08-13

The qualifier named a `test` directory that the private player package does
not have. Format stopped before analysis. The path is removed; existing app
and package Dart owners remain covered.
