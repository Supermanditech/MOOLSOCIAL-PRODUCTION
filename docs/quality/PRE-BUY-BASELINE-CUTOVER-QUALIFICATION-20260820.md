# Pre-Buy baseline cutover qualification

State: `cursor_reconstruction_pending`

This is the exclusive qualification output for
`/root/pre_buy_baseline_b0` after `PRE_BUY_BASELINE_CUTOVER_READY`.

Cursor must reconstruct only in worktree
`codex-cursor-baseline-reconciliation` on branch
`reconcile/codex-cursor/pre-buy-baseline-v1`, admit owners only through the
exact source manifest, preserve every frozen original-checkout byte, exclude
all secret/runtime-value files, qualify the clean baseline, create one baseline
commit and annotated tag `moolsocial-mvp-pre-buy-baseline-v1`, and report their
exact identities here.

No push, main, authentication, provider, build, Play, OPPO, external action or
Buy implementation is authorized by this owner.
