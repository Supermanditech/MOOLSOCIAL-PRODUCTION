# C30T protected PowerShell `$HOME` variable collision

- Date: 2026-08-13
- Device: OPPO CPH2375, serial `2b3e0f71`
- Installed artifact: Google Play Internal Testing `1.0.0-r60.44 (2026081244)`
- Scope: navigation-only comprehensive journey inventory

The unique `Social, current domain` target was selected from the fresh Chat menu and tapped once. The following verifier attempted to store matching nodes in `$home`. PowerShell treats `$home` as the protected read-only `$HOME` variable, so verification stopped after the intended transition.

No second tap is allowed. The current OPPO state must be freshly captured read-only and Social Home must be asserted using a task-specific variable such as `$socialHomeNodes`. No system environment-option name may be reused.
