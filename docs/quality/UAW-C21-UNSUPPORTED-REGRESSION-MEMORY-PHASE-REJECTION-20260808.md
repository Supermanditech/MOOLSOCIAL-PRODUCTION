# C21 unsupported regression-memory phase rejection

Date: 2026-08-08

The first successor-audit memory check passed `-Phase planning`, which is not a
supported gate value. Parameter binding rejected the command. The corrected
preselection/audit invocation uses the literal supported phase `general`.
