# C27F preselection — uniform-navigation OPPO qualification

Date: 2026-08-10

## Customer outcome and classification

C27F is `mvp_required` device acceptance work. One unique r60.26 successor may
be built only after fresh prebuild, machine and device audits, then installed
in place on connected OPPO CPH2375 and left pending founder visual acceptance.

## Qualified source and protected predecessor

C27E passed two complete unchanged-source cycles on fingerprint
`3096AC375EC1D4E55C5EB4D39602B0902EE509C8A3AD45E815D3AB0D1CA462C5`.

The protected installed predecessor is r60.25 / `2026081025`, checksum
`21C7AE3B58C9FA7C662E768FC20662228FF45DE79C4148CDC341B4DB17CB60C8`.
Its signer and first-install identity must be reconciled before replacement.

## Smallest complete device scope

1. reconcile branch, HEAD, path-safe dirty tree and connected unlocked OPPO;
2. prove r60.25 version, checksum, signer and first-install identity;
3. seal both C27E cycles and a fresh source aggregate manifest;
4. prove r60.26 evidence and candidate paths do not already exist;
5. open exactly one profile build authorization and use the existing wrapper;
6. validate package, version, signer, checksum and consume build authority;
7. open one install authorization after a fresh awake/unlocked/process gate;
8. use exactly one `adb install -r`, with no uninstall, clear or downgrade;
9. prove local, live and pulled checksum plus preserved first-install identity;
10. capture every family root/action plus switcher, Back, swipe, outside, Chat
    and content continuity;
11. require exported rail controls to be at least 44 logical pixels and verify
    Mool is visibly painted on every root before opening the switcher.

No second build/install, runtime or screenbook mutation, credentials,
messages/calls, funds, Production, commit, push, deploy or promotion.
Preselection itself opens no build or install authority.
