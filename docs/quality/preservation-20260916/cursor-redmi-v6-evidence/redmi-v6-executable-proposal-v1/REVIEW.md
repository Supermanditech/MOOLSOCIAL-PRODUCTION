# Exact executable admission proposal — approval pending

Review admission.patch and manifest.json beside this file. All five proposed files are under proposed/. These are external proposal artifacts, not an alternate working repository. No additional changes were applied to the real worktree.

This executable proposal narrows the earlier specification: checker/policy changes occur only in the existing primary coordination_bootstrap phase. The operational Redmi subagent task receives exactly 12 docs/quality audit/build-evidence owners and no app, script or config owners. Its active claim replaces the inherited historical Cursor claim only in this new worktree's configuration, preserving the one-open-Cursor-task rule. All original worktrees/branches and historical continuation entries remain unchanged. The primary's existing checker owners remain primary-owned; only the exact task document is added to that claim. Global lane restrictions and primary-only rules are unchanged.

Proposed changes:
- Include the already approved UI-lock addition exactly as it currently exists.
- Add only the new exact root/branch plus V6 source/ancestry checks to protected-Buy's existing integrated-review admission. Its reviewed source remains 10fb79b4469203371edf888e7d4b8aacb3546581. Existing exclusions, native hashes and original ancestry checks are unchanged.
- Add one exact V6 continuation (75 to 76); accept its new date only for its exact ID; validate every field and exact bootstrap owner set.
- Admit only the literal 12 pending evidence paths for this task/root/branch/ticket. Require that exact operational claim. No wildcard owner, new security suppression or generic scripts permission.
- Add the task evidence document. It records no fabricated build/test completion.

Validation already completed:
- All three proposed PowerShell scripts parse with zero errors.
- git apply --check passes for the four not-yet-applied files against the actual new worktree.
- The existing approved UI-lock addition is independently checked by reverse apply-check; it must match exactly.
- Fourteen isolated tests of the actual proposed exact-binding guard pass: valid identity accepts; missing/duplicate binding, wrong task/role/lane/branch/root/baseline/ticket/subject, added or removed bootstrap owner, and disabled integration requirement reject. Results: continuation-unit-results.json. These are isolated guard tests, not full production-coordination acceptance.
- Proposed configuration contains 76 continuations, exactly one Cursor active task and exactly 12 operational owners.
- Prior full actual UI-lock pass and ten native/location guard tests remain preserved outside this folder. The earlier original failure remains a failure.

After explicit approval, required sequence:
1. Revalidate actual source and proposal hashes; stop on drift. Apply only the additional four proposed owners, preserving the approved UI-lock change.
2. Run actual existing bootstrap validation with only the exact five bootstrap owners and inspect terminal success before commit. No commit after rejection, no metadata impersonation and no predicate changes to overcome another failure. Existing bootstrap subject/parent/owner equality remains mandatory.
3. Run full actual protected-Buy, UI-lock and unchanged backend/data-egress/brand/Windows review checks, with the already accepted source argument. These preserve the documented production/privacy/provider limitations.
4. Record admission-only SHA separately from V6. Continue existing build-record creation from real evidence, normal commit/push/clean remote checks and the single authorized Redmi build only if all prerequisites pass.
5. Verify unchanged V6 app/test/native/assets/dependencies, exact APK package/version/signer/hash and device preservation before installing. No OPPO or product implementation.

Full lifecycle checks cannot be claimed passed before applying the approved candidate and obtaining its real bootstrap commit. This proposal is syntactically valid and patch-applicable; it is not an APK qualification or permission to adjust later blockers. Any additional predicate, ownership or boundary change remains outside this diff.
