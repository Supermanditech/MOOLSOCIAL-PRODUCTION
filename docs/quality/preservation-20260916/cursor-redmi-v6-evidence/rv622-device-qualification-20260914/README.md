# Remaining r66.22 Redmi qualification

Founder authorized the remaining D005/C01, D009, D010 and D014 verification goal in this chat. Preserve18 previous original closures. Registry contains one deduplicated child, D005-C01, implemented in r66.22 but initially device-open. Starting branch work/cursor-ui/redmi-v6-audit-20260913, HEADd0dfea24e52c3c60c41d0cd01fcdcd2badfbcd6c, zero-byte Git status. Coordination, memory/device and incremental gates passed with49owners/4584registry entries. Prior mandatory coordination and memory readings remain applicable; no source/policy change occurred since their prior complete reading.

Device TG8HCYTGGQT885OF reports1.0.0-r66.22-cursorreview/code2026091403. Installed base.apk SHA2564EC88FAF583797B60220CC2795CB5690D1F8779130B037CCCF75E98AA1699A16 matches delivery. Font scale1.0. Ordinary explicit MainActivity launch without URI succeeded COLD; this is not a native-link test or a bypass of the three link-delivery blockers.

Capture001 preserves the starting Shop state. Initial capture stdout exceeded a small output budget, while complete PNG/XML/metadata remained on disk. Subsequent helper output is bounded to file paths/counts; specific XML fields and actual images are inspected separately. MIUI uiautomator emitted its existing theme_compatibility.xml warning but returned a complete hierarchy with exit0; later stderr is retained separately. No truncated output is treated as complete evidence.

Capture003 PNG occurred during the profile-to-preferences transition while its later XML described the destination. It is retained as transition/setup evidence, not a settled preferences pass. Restore the historical helper's900ms pre-capture settle and take a new immutable settled capture before preference testing; no contradictory PNG/XML pair is accepted as state proof.

English is the original selected preference (004/005); selecting Hindi yields the honest Hindi preferred / App screens: English summary (006). Return to Shop is preserved (008). The first restart command omitted Android's `am` command before `force-stop`; Android rejected it as inaccessible/not found, and no relaunch or persistence result occurred. cold-hindi-01.log preserves this CLI invocation failure. Correct invocation uses `am force-stop` for this exact package, proves process absence, then ordinary MainActivity startup without a URI. This is not an automatic approval rejection or native-link test. Capture settle was conservatively increased to2000ms for subsequent frames;008 is visually confirmed settled Shop.
