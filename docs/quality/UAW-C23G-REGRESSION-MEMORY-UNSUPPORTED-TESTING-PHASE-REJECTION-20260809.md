# C23G regression-memory unsupported phase rejection

The stale-key inventory completed, then a combined command invoked regression
memory with `-Phase testing`. The gate's ValidateSet admits only `general`,
`implementation`, `build` and `device`, so it rejected. No runtime or device
mutation occurred. REG-20260809-576 fixes C23 host test work to
`-Phase implementation -BuildMode none`.
