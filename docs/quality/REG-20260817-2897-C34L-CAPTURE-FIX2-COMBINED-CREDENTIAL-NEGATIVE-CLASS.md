# REG2897 — C34L capture FIX2 combined credential negative class

- Status: registered next PowerShell 7 combined failure; no WinPS/retry/diagnosis.
- Failure: the forbidden credential-shaped-field negative was fail-closed but outside its legacy expected retained-evidence class after exact schema/privacy strengthening.
- Root cause: the combined fixture oracle was not aligned to whether exact unknown-property rejection or forbidden-property/value rejection occurs first.
- Prevention: capture only the sanitized actual class after registration; redesign the fixture to isolate credential-name/value privacy without simultaneously violating exact schema, or explicitly classify the exact earlier schema rejection while retaining a separate privacy-value negative.
- Containment: no mutation, WinPS, retry, device, provider, private, release, or external action followed.
