# C10C Buy-local auto-reveal fought manual swipe

The first local-rail implementation scheduled selected-item centering from every build. A manual swipe changed overflow-cue state, rebuilt the rail and pulled it back to Shop. Auto-reveal now runs only when the selected destination identifier changes; cue updates preserve user scroll position.
