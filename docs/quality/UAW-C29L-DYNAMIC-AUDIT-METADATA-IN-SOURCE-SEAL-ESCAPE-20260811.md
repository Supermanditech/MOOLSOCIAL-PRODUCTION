# C29L dynamic audit metadata in source seal escape

The corrected `20260811-02` cycles sealed executable source together with the
living regression registry, regression memory, ticket scope and protected
device metadata. Registering a later read-only CLI syntax mistake therefore
changed the aggregate even though no Flutter product, backend, test or gate
source changed.

The retained `-02` evidence is not overwritten. The final qualifier separates
the domains: stable executable product/backend/test/gate owners form the
source aggregate, while ticket, scope, regression and protected-device files
are gate-validated and recorded as individual SHA-256 snapshots in each cycle.
Two final identical cycles are required in the `20260811-03` evidence set.
