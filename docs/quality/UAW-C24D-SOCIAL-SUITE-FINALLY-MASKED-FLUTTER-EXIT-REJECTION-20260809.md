# C24D Social-suite finally-masked Flutter exit rejection — 2026-08-09

The first 34-file Social/YouTube test command restored its working directory in
`finally`, and that successful `Pop-Location` masked Flutter's nonzero result.
The visible output still truthfully reported 17 failures.

Every grouped retry captures Flutter's native exit code before cleanup and
propagates it after the location is restored.
