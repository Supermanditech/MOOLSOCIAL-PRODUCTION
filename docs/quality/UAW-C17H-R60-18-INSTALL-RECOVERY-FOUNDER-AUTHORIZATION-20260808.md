# C17H r60.18 install-recovery founder authorization

The founder explicitly authorized continuing until OPPO testing and will decide acceptance after reviewing the installed result. After the C17G install command failed locally before transmitting bytes, this authority selects one path-safe install-recovery attempt for the already built r60.18 APK.

No new build is authorized. The shorter generated APK may be used only after it is re-proven byte-for-byte identical to the reserved C17G candidate. Exactly one C17H `adb install -r` attempt is permitted after every live preinstall gate passes. Uninstall, data clear, downgrade, a second C17H attempt, runtime mutation and promotion remain forbidden.
