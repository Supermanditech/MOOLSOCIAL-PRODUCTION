# C30S dependency log wrong-attempt timestamp lookup

Date: 2026-08-13

A read-only follow-up guessed the generated cycle-attempt timestamp and could
not find the dependency log. Evidence paths are now resolved from the actual
preserved directory inventory rather than reconstructed.
