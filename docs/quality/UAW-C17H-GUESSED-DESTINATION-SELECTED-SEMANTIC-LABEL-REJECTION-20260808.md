# C17H guessed destination selected-semantic label rejection

Buy/Shop qualified with two stable hierarchy confirmations. The next action used the verified `Open Wholesale` semantic bounds, but the helper then waited for an inferred `Wholesale, current` label that had not been inventoried from the destination. It timed out and saved no Wholesale q-prefixed screenshot.

Every previously unseen destination now receives an inventory-only post-tap read before any selected-label assertion or capture. Literal destination semantics, not inferred wording, control evidence naming.
