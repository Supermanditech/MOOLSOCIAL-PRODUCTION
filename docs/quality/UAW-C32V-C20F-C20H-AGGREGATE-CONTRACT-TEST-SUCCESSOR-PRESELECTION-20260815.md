# C32V C20F C20H aggregate contract test successor preselection

C32R reproduced C20F as two passes and two failures. The test requires a
`c20f` state marker after the preserved machine state advanced to exact C20H,
and freezes a C10E title that changed from local/global to
contextual/connected wording. Current C10E behavior passed 8/8.

C32V permits only those exact aggregate-test token migrations. The six-family
and seventeen-state counts, owner inventory, host-cycle rules, and closed
build/install authorities remain. Machine state and runtime are read-only; all
live and release actions remain closed.
