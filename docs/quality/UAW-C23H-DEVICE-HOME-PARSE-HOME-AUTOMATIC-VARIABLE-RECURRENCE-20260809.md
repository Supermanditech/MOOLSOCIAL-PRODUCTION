# C23H device Home parse automatic-variable recurrence

Date: 2026-08-09

The first installed-r60.22 Mool journey successfully found the single launcher,
tapped its live bounds once, transitioned to Home, and pulled unique
`17-home-initial` PNG/XML evidence. The subsequent read-only parsing statement
assigned the XML document to `$home`. PowerShell variable names are
case-insensitive, so this collided with the read-only `$HOME` automatic
variable and rejected the remaining summary output.

The device transition and already-pulled captures remain valid and are not
repeated or overwritten. Corrected parsing uses `$moolHomeHierarchy` and reads
the existing literal evidence files. Ticket-specific PowerShell variable names
remain mandatory for all later device steps.
