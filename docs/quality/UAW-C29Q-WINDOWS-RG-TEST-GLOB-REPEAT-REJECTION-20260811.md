# C29Q Windows rg test-glob repeat rejection

The promotion inventory repeated a shell-style test wildcard on Windows. The command's partial matches were rejected, and all later C29Q discovery uses literal directories with rg-owned glob filters.
