# C30T PowerShell `return $false` tokenization recurrence

- Date: 2026-08-13
- Device: OPPO CPH2375, serial `2b3e0f71`
- Installed artifact: Google Play Internal Testing `1.0.0-r60.44 (2026081244)`
- Scope: non-writing Create-format journey inventory

The Carousel-format inspection used a compressed PowerShell filter containing `return$false` instead of the valid `return $false`. PowerShell emitted non-terminating command-not-found errors for nodes that should have been rejected early. The remaining full predicate still admitted exactly the unique enabled clickable `Carousel / Carousel` node; its centre was derived from the fresh hierarchy and tapped once. The resulting evidence shows `New carousel` with the Android Google Photos picker open. No image was selected and no Create or backend write occurred.

Further device action is gated on setting `ErrorActionPreference` to `Stop`, using readable token separation, avoiding `return` in the filter where possible, re-proving the current focused picker state, and closing it with one reversible Back action before inspecting the next format.
