# C24D C16E pre-format patch-context rejection — 2026-08-09

The first semantics cleanup patch reused test context from before Dart
formatting. The current file had compacted a multiline finder, so the compound
registry/evidence/source patch was rejected without mutation.

The permanent correction reads the current formatted owner and keeps durable
regression registration separate from the exact test patch.
