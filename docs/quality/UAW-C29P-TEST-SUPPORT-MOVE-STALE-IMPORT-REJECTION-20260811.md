# C29P test-support move stale-import rejection

Two focused Social tests retained an unused production-gateway import after the review fake moved under test support. Targeted analysis rejected the warnings; the imports were removed before any qualification cycle.
