# UAW-CODEX-YOUTUBE-BACKEND-CONTRACT-REPAIR-20260824

Founder date: 24 August 2026 IST
Lane: `codex_auth`
Work ID: `youtube-backend-contract-repair-20260824`
Branch: `work/codex-auth/youtube-backend-contract-repair-20260824`

## Objective

Repair only the proven YouTube backend contract drift that blocks full package
typecheck: owner-connection-status capability admission, shared catalogue data
client search, shared catalogue provider-service composition and public Shorts
catalogue dispatch.

## Scope

- Change only the exact backend owners and corresponding tests in the machine
  claim.
- Preserve the accepted Google r60.87, Email Link and Facebook paths and both
  qualified YouTube Connect return fixes.
- Preserve OAuth state, PKCE, token vault/redaction, scopes, public/private
  capability separation, quotas and fail-closed provider behavior.
- Run focused tests, `tsc --noEmit`, full existing `npm verify` and export
  verification without deployment or external mutation.

## Exclusions

- No catalogue feature expansion or new provider operation.
- No UI, Flutter, Android, OAuth exchange, return URI, secret, provider-console,
  Cloud, deployment, build, device, private login, upload or Play action.
- Do not weaken or skip backend verification.

## Acceptance

The exact missing contracts are implemented with focused negative coverage,
full backend typecheck/tests and existing export verification pass, and the
clean repair commit can unblock restoration of the preserved package-gate
feature-mode patch.
