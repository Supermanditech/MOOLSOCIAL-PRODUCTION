# C10C compact dock glass-border tap target

The first compact shared dock rendered 42-pixel action targets because its 48-pixel surface budget subtracted both 2-pixel padding and the glass border. The inset is corrected so every rendered keyed navigation action remains at least 44 by 44 pixels, enforced by the design-system test.
