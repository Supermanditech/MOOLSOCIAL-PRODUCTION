# C33A C20E current local-destination rail test successor preselection

## Exact finding

C20E passed its inventory case and failed five widget cases. Four failures
require compact clusters centered in the rail even though C27B and current
source require sparse local actions compact and leading. The fifth casts the
removed per-cell selection/glass owners to `AnimatedContainer`.

C22G already records C20E's selection and typography contracts as superseded.
Current C27B passes 5/5, C27D passes 1/1 and R06 passes 12/12. Production
`MoolLocalNavigationRail` uses `Alignment.centerLeft`, 152/232-pixel clusters,
58-pixel destination cells, a `SizedBox` selection owner, `AnimatedScale` for
press state and one `AnimatedContainer` selected indicator.

## Smallest complete scope

Change only C20E assertions and add one fail-closed C33A gate. Preserve all
four families, all nine actions, every width/text-scale/selected-state case,
minimum targets, semantics, inert selected actions, one-tap outcomes and
reduced-motion checks. Require current leading geometry and explicitly reject
the removed glass, BackdropFilter, FittedBox and inner-chroma owners.

C17D/C21E's independent 0-pass/10-fail result remains REG-2300 and is excluded
from C33A. It requires a separate ticket. Tiffin remains inventory-only.

Runtime, references, backend, build, Play, OPPO, providers, credentials,
funds, email, quota submission and every external action remain closed.
