# UAW C30T PowerShell protected HOME variable device-harness recurrence — 13 August 2026

The first bounded Feed guest-interaction harness assigned a hierarchy path to `$home`. PowerShell treated it as the protected `$HOME` variable and stopped immediately after relaunch/evidence capture, before opening Feed or tapping any Feed action. The created XML is retained. Future commands use task-specific variable names and never assign `$HOME`, `$home` or `$CODEX_HOME` in any case.
