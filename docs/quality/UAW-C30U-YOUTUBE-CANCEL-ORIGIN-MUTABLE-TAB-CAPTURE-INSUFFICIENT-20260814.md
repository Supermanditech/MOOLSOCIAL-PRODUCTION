# C30U YouTube cancellation origin mutable-tab capture insufficient

Capturing `_tab` before the dialog still produced the Social root because the
account control was composed from Home. The provider workflow, not presentation
tab state, owns the recovery route.

The action now uses the explicit Videos recovery path. No release mutation
occurred during the failed test.
