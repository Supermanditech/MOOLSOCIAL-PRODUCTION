# Fresh Redmi candidate reconciliation

## Current bounded execution authorization

Founder explicitly authorized solving necessary APK blockers, narrowly and
without governance or policy work. Reuse the existing Cursor debug wrapper,
machine-state schema, source admission and isolated package. Observable outcome:
the reconciled Buy fixes installed on Redmi with matching APK checksum. This is
launch-supporting device qualification, not Store integration or live commerce.

Exact blocker repair: add one immutable snapshot alternative to the existing
Cursor source-admission function. Bind the exact root, branch, HEAD, SHA-256 of
reconciliation.json, complete input inventory and each file hash. The existing
accepted baseline and historical alternatives remain unchanged. Existing brand,
backend and clipboard checks receive only this qualified snapshot identity;
their structural checks and previous restrictions remain. No moving-HEAD or
arbitrary-source allowance. No policy/configuration-owner changes or commit.

The first unqualified brand invocation failed its inherited theme projection;
retained brand.log. The exact screen diff from the former admitted source changes
delivery visibility/compactness, Store return copy and cart totals only; arrival
sound/import, clipboard and canvas integration remain unchanged. The qualified
retry binds the current screen hash and retains the structural assertion.
Original source-admission rejection is retained above; new evidence does not
retroactively relabel that attempt passed.

Candidate cycle 1 diagnostics (original log retained): Saved filter fixture
counts mixed fulfilment modes; delivered quick-1 is incorrectly expected in the
active rail by the old test; the 711px/2x delivery picker test taps a scrolled
tile under another surface; Saved scroll restoration selects mixed-mode items
and no longer produces the required overflowing list. These are inspected before
any expectation change. Preserve active-only delivery, sale-type filtering,
hit-testability and nonzero exact scroll restoration assertions. No app-source
change is justified by an obsolete expected state alone.

Founder relayed Codex's planned versioned Store-record mapping handoff. No
handoff dataset has been supplied in this worktree yet. This APK continues on
the reconciled source; authentic Store-derived data and later integration remain
explicitly pending. Do not replace them with independently invented Buy data.

Pre-APK sequencing clarification: founder relayed a concrete cross-side pickup
mismatch and asked whether Store-provider testing should precede the APK.
The versioned Store-record/mapping handoff must now be checked before building;
its exact path and revision were requested. Continue the already running local
regression and necessary test repairs, but do not issue the build authorization
until this handoff check is accounted for. Earlier founder intent that all stores
offer pickup remains recorded; do not silently reinterpret a Store disable flag
or hide that disagreement in successful fixtures. No Store-owner edit or new
provider implementation is authorized by this note.

## Qualification results retained before the Store handoff

- Full Buy cycle 1: 2439 passed, 27 skipped, 30 failed; 25 minutes 11 seconds.
  Log: apps/mobile/build/review-candidates/cursor-buy-post-redmi-20260922-r1/buy-cycle-1.log.
  This is a failed qualification run, never a pre-build pass.
- Corrected targets r1: 30 passed, 8 failed. Remaining cases use a canonical
  Saved product rather than the originally selected variant, and the next order
  from the active delivery list rather than an assumed scheduled order.
- Seven test owners were corrected for the intended existing behavior: same
  sale-mode Saved fixtures, nonzero Saved scroll restoration, active-only rail,
  centered hit-tested picker taps, foreground Offers border, compact collection
  copy, and settled Cart parking. No runtime change was made during these runs.
- Two additive candidate Cart captures retain every original reference. Pixel
  differences are confined to the aggregate label changing Subtotal to All
  baskets. They are local candidate comparisons, not founder/Redmi acceptance.
- Flutter wrote eight new golden diagnostic images into previously tracked
  failure paths. Those diagnostics were archived under cycle1-golden-diagnostics;
  original tracked bytes were restored only after verifying their HEAD hashes
  matched the initial reconciliation snapshot. Both evidence generations remain.
- A Python default-codepage read failed after copying the two candidate PNGs;
  no test-source mutation occurred in that failed read. Retry used explicit
  UTF-8 and preserved the already copied candidate images.
- Existing source, brand, backend-review, data-egress and UI-lock checks passed
  for the initial exact snapshot. Build regression memory passed (4616 entries,
  238 applicable). Wrong source commit was rejected by the source gate.
- Test repairs now change that snapshot. Re-seal the final source inventory and
  exact candidate registration after the Store handoff; do not reuse the earlier
  fingerprint. Two successful full cycles remain required before the APK.

Final focused disposition before waiting for handoff: all 30 originally failing
cases now pass across the corrected reruns. R2 passed all four delivery profiles;
its eight Saved failures were caused by the repair's incorrect single-variant
assumption, retained in corrected-targets-r2.log. R3 uses the actual saved
catalogue listing and explicit all-category context, and passed all eight notice
journeys (21 seconds). Other corrected cases passed in R1. No failed assertion
was removed or skipped. Changed-test analysis reported no issues.

Current inventory: reconciliation-after-test-repairs.json, 3187 files,
SHA-256 70578ddfb5b6fa8a735fe2e56bc94b4e51cde52085e4e195e99179e3d528d7b3.
Runtime, assets, native and shared-contract hashes still match the initial
snapshot. Differences are test repairs, additive candidate images and the exact
existing-source registration blockers described above. Prior tracked failure
images and generated platform support files are unchanged after restoration.

Machine state is awaiting_versioned_store_handoff; build authorization remains
pending_required_gates. No build/install/commit/push has occurred. Next: inspect
the delivered Store mapping and versioned fixtures, resolve the cross-side pickup
disagreement against founder intent, then seal final source and run the required
successful full regressions before the fresh Redmi build and checksum check.

Founder authorized a fresh Redmi APK and required every completed commit and
local change to be included. Proposed candidate:
UAW-CURSOR-BUY-POST-REDMI-20260922-R1, version 1.0.0-r66.32, code 2026092201.
This supersedes the earlier no-build boundary for this candidate only.

Branch work/cursor-ui/buy-ready-20260921; HEAD
87bc96d4c28300146c9e2c3c3b37c7c3aacffed0. The original starting checkpoint
06432e2b946b02007d9946c562f922294f485632 is an ancestor of HEAD.

The existing approved-integration coverage checker passed all 18 required tips
and excluded both rejected tips. Fourteen commits after the starting checkpoint
are inventoried. Nine modified tracked files and two new untracked source/test
files are included, including buy_v2_customer_copy.dart and its new regression
tests. A committed-only build would omit those latest fixes.

Exact commit list, changed-file inventory and SHA-256 hashes of current runtime,
assets, native, dependency, test and shared-contract inputs are retained at:
apps/mobile/build/review-candidates/cursor-buy-post-redmi-20260922-r1/reconciliation.json.
This inventory is preparation evidence, not a passing machine-state record.
It does not claim inclusion of unfinished or unintegrated work in other lanes.

## Exact remaining build admission blocker

The mandatory scripts/check-buy-protected-baseline.ps1 rejects the current tree:
"Protected Buy baseline is missing and no exact sealed overlay applies."
Its existing Cursor storefront source admission is pinned to
d5279222466211f0526c625e58b8da5dc0d78218 and requires zero source delta and no
untracked source. Current completed fixes necessarily differ from that old pin.
The prior r66.31 record cannot qualify r66.32. No checks, policies, protected
references or source admission code were changed to bypass this rejection.

The exact current source needs additive review-source admission before the new
candidate can pass its build gates. Local implementation evidence is preserved
in CURSOR-BUY-POST-REDMI-FIXES-20260922.md. Full fresh-candidate regression,
build, installed checksum and device replay are still pending, not passes.

Redmi TG8HCYTGGQT885OF is connected and still has version
1.0.0-r66.31-cursorreview, code 2026092101. No APK was built or installed in
this preparation. No commit, push, merge, reset or other-worktree change occurred.

## Store handoff received: static review, 22 September 2026

Read Codex's CURSOR-STORE-PUBLIC-DATA-HANDOFF-20260922.md and its exported
store-public-test-handoff-v1.json in the supplied Store worktree, read-only.
Export SHA-256: b8160b885019523d76f6c6c9e3574ed42f87c26830fbe5c761717b24838c80fd.
All seven recorded Cursor source fingerprints still match this checkout.

65 static consistency checks passed: eight distinct cases, exact Store/SKU/
canonical/variant/pack identities, media bindings, Wholesale price/MOQ,
two channel projections, and explicit non-live order state. Evidence:
apps/mobile/build/review-candidates/cursor-buy-post-redmi-20260922-r1/store-handoff-static-audit.json.
These are exported-data checks, NOT Dart runtime tests or public-screen replay.
Codex's reported 22 contract and six interaction passes were not rerun here.

The handoff explicitly requires coordinated source integration before using its
shared Dart fixtures. Its Store increment remains uncommitted and full public
consumption is incomplete. No Store files were copied into runtime or changed.
The invalid photo URLs cannot provide device image-rendering acceptance.

Cross-side decision remains unresolved: founder previously required pickup for
all Stores; the new handoff requires pickup Off to disable it. Current Buy follows
the earlier founder rule. Do not silently reverse that rule or mark the disagreement
fixed. Confirm the intended shared contract before changing either lane.

Receipt supersedes the earlier statement that the handoff file is missing.
Runtime integration/replay and final APK gates remain pending. No APK built or
installed; no new claim that all Store-to-checkout findings are closed.

## Authorized compact Store address and Maps entry, 22 September 2026

Founder postpones Store integration dependencies and directs a fresh Redmi APK
followed by replay of the roughly 33 implemented defects. Existing all-Store
pickup behavior stays unchanged for this review; shared pickup decision deferred.

Launch-supporting public customer outcome: locate the chosen Store before visiting.
Reuse BuyV2StoreListing.address and existing Store-info/collection checkout owners.
Add one compact shared address/Google Maps control; no invented location, new
Store model, automatic message, backend, payment, policy or other-lane changes.
Maps opens only on the customer's tap using Store name/address, never buyer data.
Missing address has no map action; launch failure offers retry through the same tap.
Acceptance: encoded address/identity, missing input, failure, small-screen/large-text
layout, existing Buy regressions, then Redmi screenshots and defect replay.
Store-derived live publication and photos remain explicitly deferred.

Qualification note: the build-foundation check internally invokes Flutter and
was rejected because the full Buy run already held the clean-support lock.
No app mutation or bypass occurred. Run that check sequentially after the suite;
retain build-foundation.log as the failed invocation evidence.

## Current candidate qualification after address change

Full Buy run buy-cycle-2.log passed: 2472 tests, 27 inherited skips, zero
failures (27:09). New address tests passed 3/3 after fixing the 48px touch target.
Flutter analysis returned 1 solely for the same three inherited informational
brace notices; Dart analysis returned 0 with those same notices, no warnings or
errors. New files are format-clean. No analysis rule or test was disabled.
Build-foundation r2, clean-support, package/profile, UI locks, 18 approved-history
coverage, exact source, brand and review boundaries passed. Wrong source rejected.

The two existing boundary checkers admit only the exact hashed address widget
for this isolated candidate after full source qualification. Its user-tapped
Google Maps action sends Store name/address only, no buyer data or message.
This is an additional documented review exception, not backend/production acceptance.
No policy/configuration or other-worktree edits.

Snapshot reconciliation-final.json SHA-256:
C5560F3A1472B265528589A928FF17343BBDAB73288C7E32A5EE3438BE6F8D5F.
Final source manifest: 3233 files, SHA-256
75D1B6F69EEE462C70C1205E37011F4F4EE9B461CF006235A50D0A2398DAE73D.
Both live fingerprints must still match before the wrapper build.
Second successful full run is pending in buy-cycle-3.log. No APK yet.

## Redmi replay completed — 22 September 2026

Fresh r66.32-cursorreview (2026092201) installed successfully with retained data.
Built APK equals pulled installed base.apk SHA-256:
F74ADCD10DE5A5DFA3F6A29F024484DCB8CAF294FB0682C01680EB7190F2CF8A.
Both full required cycles passed 2472 / 27 inherited skips / 0 failures.
Post-replay verification confirms all 3233 pinned inputs unchanged.

Final bounded replay report: CURSOR-BUY-R6632-DEVICE-RETEST-20260922.md.
31 unique originals: 21 closed within observed review scope; 10 remain open
with linked child/dependency records. The two initial reports are duplicates.
9 child defects and 3 dependency/retest records; inactive rail zero-active state
remains explicitly unqualified. Online Google Maps search works, but authentic
Store destination remains postponed/unverified. No production acceptance.
Founder explicitly instructed stopping after all originals are closed or carried
with a report. Replay stopped; no newly reported issue implemented here.
