# C10C context-header unscoped copy assertions

Header tests globally rejected text such as `Flexible packs`, although that wording is truthful destination-local catalogue content. The prohibition applies only to visible context-CTA copy inside the cinematic header, so absence queries are now scoped to that header owner.
