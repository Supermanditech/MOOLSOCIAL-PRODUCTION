# C29P unbounded UI v2 inventory truncation

A recursive UI v2 inventory included thousands of image artifacts and truncated. It was not used as test evidence. The protected suite now uses an explicit bounded list of Social Dart test owners.
