# C10C compact Buy Orders direct off-screen tap

Readable 60-pixel local cells make Orders horizontally reachable by swipe on a 320-pixel header. The old test tapped its off-screen centre directly. Compact coverage now asserts the overflow cue, performs a real rail swipe, then taps Orders and verifies the destination.
