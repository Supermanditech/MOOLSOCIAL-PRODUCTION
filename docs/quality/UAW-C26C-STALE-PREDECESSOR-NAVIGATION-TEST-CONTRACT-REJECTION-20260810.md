# C26C stale predecessor navigation test contract rejection

## Observation

The first focused C26C suite had two predecessor assertion failures: the old exact 44-by-44 Mool launcher size and the removed close-button key.

## Cause

The shared Flutter owner had moved to the founder-approved compact rail dimensions and chrome-free dismissal contract, while one focused predecessor test still encoded the replaced C24 presentation.

## Permanent prevention

- Search the focused suite for removed navigation keys and superseded exact dimensions before retry.
- Require minimum 44px accessibility instead of the obsolete exact C24 size.
- Validate outside tap, swipe-down and system Back dismissal; never restore close chrome.
- Add a C26C machine gate and focused widget test for the immutable approved interaction contract.

## Resolution evidence

The rejected output is preserved in the current task transcript. The predecessor test and new C26C contract test are migrated before the next suite run.
