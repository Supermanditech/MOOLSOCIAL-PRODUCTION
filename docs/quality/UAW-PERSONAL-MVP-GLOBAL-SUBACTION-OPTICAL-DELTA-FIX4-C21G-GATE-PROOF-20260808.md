# C21G optical-delta gate proof — 2026-08-08

The machine-readable optical-delta gate exists and passed its positive/negative proof. The accepted C21 contract reports 11 independently compared structural dimensions against the checksum-matched founder-rejected r60.19/C20C baseline. A mutation probe that collapses the successor back to the predecessor surface, geometry, depth, indicator, press, blur and provider-glyph contract is rejected.

The rendered-pixel proxy captures raw RGBA output inside `WidgetTester.runAsync` and passes for both light and media surfaces with minimum 8% changed pixels and mean absolute channel delta 6. The proxy is explicitly a documented-contract host test, not a production widget or screenbook copy; real OPPO pixel acceptance remains mandatory in C21H.

The positive and negative checker scripts now exist and are eligible permanent-regression owners. Build, install, runtime and device mutation remain closed.
