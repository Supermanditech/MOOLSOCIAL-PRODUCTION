# C22F1 preselection assessment — composite glass legibility

## Why this ticket is required

C22G's first corrected host cycle rejected in the required suite. After stale
C16/C20 implementation-shape assertions were migrated, the focused suite
proved a real 3.5557:1 white-label contrast failure across Social, Buy, Eat,
Ride, Book and Work on accepted bright destination probes. This directly
matches the founder's report that subaction text remains insufficiently
legible.

C22G is test/gate-only and cannot alter runtime. C22F1 is therefore selected
as the smallest authorized runtime repair before C22G can restart.

## Scope and reuse

- Reuse the single native-Flutter owners `MoolLocalNavigationTokens`,
  `_MoolInnerChromaEmission`, `_MoolLocalNavigationCellState` and
  `_MoolMiddleDockAction`.
- Reuse the existing C20D/C20E contrast probes and C22 machine contract.
- Change only neutral smoked-glass alpha, selected inner-emission alpha and
  their exact tests/gates.
- Add no screen, route, backend owner, business state, subaction, filler
  feature, panel, strap, trapezoid or screenbook code.

Duplicate search is complete: all six families already project through the
same shared token and emission owners, so no family-local styling owner is
needed or allowed.

## Frozen target

- Neutral glass top: `0xB30D1326` (70.2% alpha, 29.8% destination
  transmission).
- Neutral glass bottom: `0xAB050816` (67.1% alpha, 32.9% destination
  transmission).
- Selected radial emission: 0.28 maximum, 0.14 middle, 0 at edge.
- Press emission continues through the shared 0.62 opacity layer and 100ms
  motion; selection remains 180ms; reduced motion remains immediate.
- White icon/label composite contrast: at least 4.5:1 across representative
  white, bright-orange, media-dark and navy backgrounds for all six accents in
  unselected, selected and pressed states.
- Geometry remains 72×48, radius 24, gap 8, icon 18, label 12/800; outer and
  inter-capsule destination pixels remain untouched.

## Authority and evidence plan

Runtime source plus focused test/gate writes are authorized. Build, APK,
install and protected-device mutation remain closed. C20D, C20E, C22B, C22F,
the new composite test, analyzer and machine gates must pass before C22F1 can
close. Then C22G restarts at cycle 1 with a new unchanged-source fingerprint.

The installed r60.20 identity remains preserved throughout C22F1.
