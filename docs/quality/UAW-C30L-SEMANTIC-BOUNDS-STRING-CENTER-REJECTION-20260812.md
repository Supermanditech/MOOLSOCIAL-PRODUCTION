# C30L semantic-bounds string-center rejection

- ID: `REG-20260812-1429-C30L-SEMANTIC-BOUNDS-STRING-CENTER-REJECTION`
- Date: 2026-08-12
- Scope: bounded OPPO navigation replay
- Result: rejected; Feed remained current and no external/cloud mutation occurred

The first Create navigation concatenated regex capture strings before division, producing an off-screen tap. The captured follow-up hierarchy proves Feed remained unchanged. Future semantic taps convert every bounds capture to an integer before midpoint arithmetic and verify the midpoint is inside the node.
