# C32W R15 current Mool Home accessibility test successor preselection

C32R reproduced R15 as one pass and fifteen failures. Every failure occurs at a
`scrollUntilVisible` call scoped to the removed `mool-root-main-actions` rail,
before the test can exercise the remaining Personal fitment and recovery
matrix. Current R03 and C24B2 fixed-Home compact authorities pass.

C32W permits migration of only the two R15 root portions to fixed
`mool-home-family-*` targets and current semantics. All nonroot matrix coverage
must remain and the complete R15 file must run afterward. Any later runtime
defect requires a new registered finding and ticket. All live and release
actions remain closed.
