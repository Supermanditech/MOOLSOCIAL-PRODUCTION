# C24C six-file compatibility-batch truncation rejection — 2026-08-09

A six-file compatibility run combined intent, wording, Eat exposure,
global-motion and default-route suites. It ended with 54 failures, while the
100-line output window retained only late global-navigation failures and hid
the first diagnostics.

REG647 restores the bounded sequence: execute each historical owner alone,
register any stale C24B3 contract, migrate it, then recombine only passing
owners. No C24C runtime source is changed from this batch alone.
