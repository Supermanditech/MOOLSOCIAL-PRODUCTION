# C11 contextual sub-action thumb shelf founder authorization

Date: 2026-08-07

Ticket:
`UAW-PERSONAL-MVP-CONTEXTUAL-SUBACTION-THUMB-SHELF-FIX1-C11`

State: `FOUNDER_AUTHORIZED_FOR_PRODUCTION_GRADE_IMPLEMENTATION`

After receiving the exact ticket count, device checklist and recommended
sub-action UX, the founder explicitly approved C10A, C10B, C10C, C10D and C10E
and directed Codex to implement the recommended sub-action UX.

The accepted pattern is one lightweight destination-local contextual shelf
immediately above the unchanged founder-approved global bottom rail. Primary
sub-actions remain visible, selected and one-tap reachable in the lower thumb
zone. The top zone remains reserved for advertising, promotional video and
primary content. A More menu, modal, palette or multi-tap drill-down is not an
acceptable default.

Authority covers the smallest complete native Flutter correction, host
qualification, one unique machine-gated APK build, in-place OPPO install and
checksum-matched real-user qualification. Build and install remain fail-closed
until host gates pass and the APK machine state is separately prepared.

This does not authorize backend/provider changes, credentials, live messages
or calls, payments or funds, Production writes, commit, push, deployment or
promotion.
