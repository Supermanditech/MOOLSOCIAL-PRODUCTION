# C22B combined-state alpha assertion rejection

The maximum selected-plus-pressed top-gradient alpha is 0.550588 because the 0x78 base channel is quantized and receives the specified 0.08 state increment. The first strict `<0.55` assertion rejected by 0.000588. The corrected test checks the exact base separately and requires every combined state below 0.56.
