# UAW C16H cumulative OPPO qualification preselection — 2026-08-08

State: `selected_after_C16A_through_C16G_host_qualification`

## Customer outcome and MVP classification

C16H gives the founder one checksum-identical OPPO candidate on which every Social, Buy, Eat, Ride, Book and Work family can be reviewed as one professional, adaptive native-Flutter system. It is `mvp_required`: this is the cumulative device qualification explicitly authorized by the registered C16 parent after two consecutive host cycles.

## Reuse and duplicate assessment

- Reuse the already host-qualified `MoolLocalNavigationRail`, `MoolLocalNavigationTokens`, six existing family scaffolds, existing routes/state/copy/content owners, global rail, motion owner and reduced-motion behavior.
- Reuse the existing single-build wrapper, APK regression machine gate, in-place Android package identity and OPPO evidence workflow.
- Duplicate search found no need for another screen, route, renderer, navigation owner, backend owner or persistent state owner.
- Implementation disposition is build/evidence-only. No runtime source mutation is authorized in C16H.

## Minimum complete scope

Seal current branch/HEAD/source aggregates; pass the machine gate's negative and positive self-tests; create one unique r60.16 profile APK; validate package, version, signer, checksum and post-build source identity; install exactly once with `adb install -r`; prove installed-base checksum identity; and exercise/capture every six-family subaction state plus motion, Back, Mool, Chat and content reachability.

## Exclusions and stop conditions

No second build or install, uninstall, data clear, downgrade, runtime/reference/backend/service change, screenbook copy, filler feature, credentials, provider/customer communication, payment/funds action, Production write, commit, push, deploy, promotion or protected-runtime mutation is authorized. A material machine, signature, checksum, source-drift or device gate rejection stops C16H and is recorded truthfully.

Founder acceptance remains pending after device evidence; qualification does not promote an approved reference.
