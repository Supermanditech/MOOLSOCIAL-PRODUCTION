# C26G brand gate old text-only launcher presentation rejection

The brand gate still required the predecessor `child: Text(` launcher after the approved C26 replacement. The current Mool Home control uses the native four-square `Icons.grid_view_rounded` with the compact `Mool` label.

The complete bounded launcher token loop was audited. It now requires the approved four-square icon and exact Mool label while retaining the duplicate-custom-logo prohibition.
