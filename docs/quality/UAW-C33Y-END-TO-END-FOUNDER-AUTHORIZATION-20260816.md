# C33Y end-to-end founder authorization

Date: 2026-08-16 IST

Founder statements include `I AUTHORISE TO CODEX FOR END TO END`,
`continue i authorise`, and the explicit request after REG2638 to register the
regression and continue with C33Y r60.63 through the Play in-place OPPO update.

This authorization applies to the selected successor ticket
`UAW-C33Y-R60-63-AUTHENTICATION-NO-REGRESSION-PLAY-OPPO-ACCEPTANCE`,
version `1.0.0-r60.63` / `2026081363`. It authorizes the repository's exact
sequential end-to-end acceptance path:

- read-only Play Console route prequalification before the source seal, after
  the founder completes visible Google sign-in and any developer-account choice;
- one founder-hidden-input AAB build only after the complete source seal, two
  fresh identical cycles and the exact build gate pass;
- one upload and activation on Google Play Internal Testing only after exact
  postbuild and preupload gates pass;
- one in-place OPPO `2b3e0f71` / `CPH2375` Play update only after exact
  postupload and preinstall gates pass;
- the mandatory supported authentication, Social and whole-app device
  journeys, including one founder-entered and founder-opened passwordless email
  link only after install qualification.

Authority remains phase-gated. A later action cannot occur merely because this
document exists; its preceding candidate gate, evidence binding and one-action
count must pass. Any recurrence, new defect, post-seal discovery or source/
registry mutation rejects the candidate and requires an exact successor or
registered repair before retry.

REG2638 through REG2644 remain unwaived historical C33V prevention evidence.
REG2645 rejects C33V at 0/0/0/0 because its exact cycle orchestration was not
fully inventoried before sealing. REG2646 and REG2647 preserve the C33W
pre-seal audit and static-checker mistakes without counting either as a gate
pass. REG2648 rejects C33W at 0/0/0/0 after two passing source cycles because
final replay proved its recorded focused-manifest hash preceded the final file
bytes. REG2649 rejects C33X at 0/0/0/0 before cycle 1 because a post-seal
compound manifest-binding patch assumed identical state and aggregate layouts.
REG2650 preserves the C33Y pre-seal uppercase candidate-ID transform omission
without counting a ticket correction or gate pass. C33Y declares hash and
file-count fields in both owners before sealing, binds the verified on-disk
focused manifest before sealing, and requires separate verified post-generation
source-manifest bindings while retaining the predeclared sanitized cycle runner.

The founder alone enters Google account credentials, the upload password,
Firebase Android API key and Google OAuth server client ID. Codex may leave the
visible sign-in page and provide one literal launcher command after all gates,
but must not create the interactive launcher process. The founder pastes that
command into an already visible founder-owned PowerShell console. Codex may
not read, inspect, copy, persist, log or infer credentials, terminal
keystrokes, account identifiers, tokens, private links, codes or attestation
data.

This does not authorize another Play track, ADB/sideload/uninstall/data-clear/
downgrade, a second AAB, backend/Hosting/provider/Production deployment, real
SMS, YouTube quota or email submission, API enablement, funds, secret access by
Codex, waiver or a production-readiness claim before complete acceptance.
