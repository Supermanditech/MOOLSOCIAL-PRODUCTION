# C26H approved navigation OPPO qualification preselection

Date: 2026-08-10

## Customer outcome and classification

C26H is `mvp_required` device acceptance work. One unique r60.25 successor may be built only after a fresh prebuild/machine/device audit, then installed in place on the connected OPPO and left pending founder visual acceptance.

## Qualified source and protected predecessor

C26G passed two complete unchanged-source cycles on fingerprint `FFDA4369201743BAE65EC7D743F5A6615AA24C92B8DD36678B20F02AC30C64E9`.

The protected installed predecessor is r60.24 / `2026080924`, checksum `4B261C09AA771CDEBFCEA201A1D198EA01B6E46522826C4202A9D83150DE3BF5`, installed in place on OPPO CPH2375 serial `2b3e0f71`. Its signer and first-install identity must be freshly reconciled before replacement.

## Reuse and smallest complete scope

Reuse the existing single-build wrapper, APK machine gate, in-place ADB upgrade and OPPO screenshot/UI-hierarchy evidence workflow. Create no runtime, screen, route, backend, persistent state, family or subaction. The bounded sequence is:

1. reconcile branch, HEAD, complete path-safe dirty state and connected unlocked OPPO;
2. prove r60.24 live version, checksum, signer and first-install identity;
3. seal the C26G evidence and a fresh source aggregate manifest;
4. prove the candidate identity and evidence paths do not already exist;
5. open exactly one r60.25 profile build authorization;
6. perform exactly one wrapper build and postbuild validation;
7. open one install authorization only after a fresh awake/unlocked/process gate;
8. use exactly one `adb install -r`, with no uninstall, clear or downgrade;
9. prove local, live and pulled checksum, version, signer and preserved first-install identity;
10. exercise all six family roots and relevant local actions, Mool tap/swipe/outside/Back, Chat and content reachability, then leave founder acceptance pending.

## Explicit exclusions

No second build or install, uninstall, data clear, downgrade, runtime or screenbook mutation, credentials, messages/calls, funds, Production, commit, push, deploy or promotion. Preselection itself opens no build or install authority.
