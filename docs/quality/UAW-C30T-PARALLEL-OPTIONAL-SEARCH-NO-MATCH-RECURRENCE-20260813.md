# C30T parallel optional-search no-match recurrence

Date: 2026-08-13

A four-query audit normalized expected no-match handling for the asset-link inventory but not for an optional documentation-contract search. The latter returned ripgrep exit 1 and again invalidated the aggregate orchestration result.

Every optional search in subsequent batches must implement the same explicit exit 0/1/>1 contract. No product, backend, provider, device, AAB, Play or communication state changed.
