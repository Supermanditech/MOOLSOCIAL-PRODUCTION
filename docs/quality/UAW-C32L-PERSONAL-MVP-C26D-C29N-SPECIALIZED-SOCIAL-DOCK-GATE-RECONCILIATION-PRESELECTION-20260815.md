# C32L C26D/C29N specialized Social dock gate preselection

Date: 15 August 2026
Ticket: `UAW-C32L-PERSONAL-MVP-C26D-C29N-SPECIALIZED-SOCIAL-DOCK-GATE-RECONCILIATION`
Classification: `mvp_supporting`

The accepted C29N Social dock owns Mool at the left edge, Home, Shorts, Create
and Feed in the middle, and Chat at the right edge. C26D still demands the
removed generic `bottomNavigationBar: MoolDestinationNavigationV2(` wrapper.
All its Social action and Buy/Shop assertions remain true.

The smallest repair changes only that obsolete C26D wrapper literal to the
current specialized dock owner and adds one checker binding the exact C29N
order while retaining every Shop assertion. No runtime, UI, reference, route,
screen, backend, provider, release or device source changes are authorized.

Two identical qualification cycles must pass C26D/C32L on both PowerShell
hosts, focused C29N and C26D Flutter tests, targeted analyzer, full C28E
gate-only preflight, memory, MVP scope/delivery and UI locks.
