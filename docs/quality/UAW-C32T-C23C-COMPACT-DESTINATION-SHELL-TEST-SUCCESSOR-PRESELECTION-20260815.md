# C32T C23C compact destination shell test successor preselection

C32R reproduced C23C as one pass and one failure. The failing case requires the
removed full-width `mool-home-launcher` and rejects the local rail. Current
C26D, C27B and C27D authorities instead qualify the compact Mool launcher,
destination-local navigation and Chat edge anchor.

C32T permits migration of only the C23C test owner. Runtime navigation source,
keys, geometry, semantics, routes and copy remain read-only. The existing Mool
Home no-bottom-navigation assertion remains. All backend, build, Play, OPPO,
provider, external and secret actions remain closed.
