# UAW-C34P blocker-resolution founder authorization

Date: 18 August 2026 (IST)
Founder direction: `founder is back resume all fixes/pending tickets/ and solve blockers`

## Authorized outcome

Resume the remaining C34P public-authentication blocker work in task
`18-08-2026`. Implement, test, fix and retest the source needed to turn the
already qualified X PKCE and Facebook configuration contracts into real
fail-closed mobile adapter paths behind the existing shared Screen 03 gateway.

This authorizes the smallest necessary runtime, platform-configuration, test,
gate and backend source writes after a fresh MVP robustness/reuse selection:

- one X native callback adapter using OAuth 2.0 authorization code with PKCE;
- one privileged existing-Functions-package X exchange that verifies the
  provider result, mints a Firebase custom token and revokes the transient X
  token without storing it;
- one Facebook native SDK adapter requesting `public_profile` only and using
  Firebase credential sign-in; and
- shared gateway, rollback, protected-return and configuration tests.

## Boundaries retained

The founder-resume direction does not authorize Codex to read, type, repeat or
store any account identifier, email address, phone number, OTP, password,
private link, OAuth token, provider app secret, client token, key hash value or
account-chooser content. Synthetic test values remain non-private.

Provider-console app creation, real app/client IDs or secrets, paid X credits,
Firebase/Meta/X external writes, real email/SMS, private login, build, Play
upload/activation and OPPO mutation still require their exact later machine gate
and any action-time founder confirmation. No provider is described as live
until authoritative external readback and a separately authorized candidate
prove it.
