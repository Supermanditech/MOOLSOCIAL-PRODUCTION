# C26B completion — transparent unboxed destination rail

State: `complete`.

- One shared `MoolDestinationNavigationV2` composition now renders Mool, the
  named family root and the existing direct local actions.
- `MoolLocalNavigationRail` no longer renders capsules, BackdropFilter glass,
  gradients, specular edges, chroma emission, card shadows, borders or a
  family background strap.
- Previous and Next controls were removed from the bottom rail; system and
  visible route Back owners remain unchanged.
- Social, Shop, Food, Travel, Care and Work fit without horizontal scrolling at
  320 logical pixels and 1.4 text scale; every control remains at least 44px.
- The Mool control uses the approved four-square icon and `Mool` label; the
  family root uses its own label, icon and restrained approved accent.
- The focused analyzer, C26B machine gate and nine focused widget tests passed.

No route, session, screen, business content, backend, build or device state was
changed. C26C may now replace only the global switcher presentation.
