# UAW Personal R01-R15 cumulative OPPO qualification preselection assessment

Date: 6 August 2026
Ticket/candidate: `UAW-PERSONAL-R01-R15-CUMULATIVE-OPPO-QUALIFICATION-FIX1`
Classification: `mvp_required`

## Customer outcome and reason

A Personal user can upgrade from the protected FIX7 app and use the completed
R01-R15 Universal/Mool, exact action roots, global Chat, safe legacy recovery
and context restoration on the connected OPPO without losing app data or
regressing the accepted Buy runtime.

Physical-device truth, retained-state upgrade safety, accessibility,
interruption recovery and checksum-matched installation are launch release
requirements, so this bounded qualification is `mvp_required`.

## Reuse inventory and smallest complete scope

- Reuse the completed R01-R15 native V2 presentation, route/session owners,
  interaction contracts and deterministic tests.
- Reuse the existing strict APK regression machine gate and device-review
  wrapper. Generalize only the wrapper's candidate allowlist and require a UAW
  regression profile; do not create a second build pipeline.
- Reuse FIX7 Buy gates and two Buy regression runs because the accepted Buy
  runtime must remain production-grade inside the same app.
- Add two cumulative Personal affected-regression runs, full analysis, source
  identity and negative gate self-tests. Attempt the raw full mobile suite and
  preserve every failure. Because the repository carries known superseded
  visual/contract tests, require two additional runs across every qualifying
  non-golden test outside an exact documented historical hold list; never
  regenerate goldens or rewrite old contracts to make this candidate pass.
- Build one unique profile candidate, verify signature/badging/hash, preserve
  the installed FIX7 identity, perform an in-place upgrade and prove the pulled
  installed APK matches the built APK.
- Exercise exact Personal navigation, Back/Chat return, old-link containment,
  retained process restore, 140% text, accessibility targets/semantics,
  reduced motion, cold/process interruption, performance and runtime failures.

## Duplicate search and necessity proof

No new screen, route, state owner, service, backend owner or build family is
needed. The existing build wrapper is the only approved pipeline but its
parameter validation accepts only `BUY-*`, which would force either a raw
forbidden Flutter build or a duplicate wrapper. A narrow candidate-family
generalization plus profile-specific required gate IDs is the minimum lawful
correction and preserves one machine-controlled build owner.

## Explicit exclusions

- No R04 Social activation or R16-R45 workspace/provider implementation.
- No new customer copy, visual polish, motion, screen, route, state owner,
  backend, service, provider or Production fact.
- No screenbook, locked Screens 01-03 or protected Buy presentation change.
- No manual OPPO uninstall, app-data clear, signature workaround or downgrade.
- No credentials, external service, payment, funds, Production write, commit,
  push, deployment or promotion.

## Dependencies and verification

Dependencies: founder authorization recorded for this exact candidate;
completed R01-R03/R05-R15 owners; protected FIX7 artifact and installed
checksum; current remediation branch/HEAD; connected authorized OPPO; MVP
scope, delivery-discipline and APK machine gates.

Verification: branch/HEAD/dirty preservation; exact source manifest; format
and analysis; focused/cumulative Personal tests twice; one preserved raw full
mobile diagnostic; two qualified broad mobile regression runs; Buy regression
twice; explicit legacy-regression disposition; positive release/protected-
boundary gates; negative machine-gate tests; unique profile build; signature/
badging/version; installed and pulled checksum; retained-data launch; action/
Chat/Back/legacy/restore journeys; accessibility/text/display; reduced motion;
interruptions; performance; runtime-failure scan; post-device source identity.

## Execution finding and gate refinement

The raw `flutter test` run exposed inherited repository inconsistency rather
than a candidate-local failure set: 123 failures across 35 files, dominated by
stale pixel masters, plus seven assertions in six historical contract files
that conflict with their accepted successors. The exact raw log, isolated
reproductions, source/SDK comparison and non-waiver rules are preserved in the
candidate evidence folder. This finding does not authorize a golden update or
hide the raw red result. The machine profile now requires two green qualified
broad runs and a separate `legacy-regression-disposition` gate in addition to
the already-green Personal and protected Buy double regressions.

The interaction gate then proved one candidate-local cross-surface defect:
protected Social still emitted the two Work paths that UAW-R12 intentionally
contains. The founder authorized the exact two-string canonical-route
correction after disclosure. No Social presentation, copy, state, provider or
baseline owner is changed.

Estimated impact: **1 day**, inside the founder-locked 60-75-day window.
