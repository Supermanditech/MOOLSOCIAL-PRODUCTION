# C27C stale predecessor switcher-presentation literals rejection

## Observation

After C27C passed its successor gate, the C26C predecessor gate rejected the
new shared row-height token because it still required hardcoded `height: 56`.
The same gate also retained predecessor hardcoded blur, alpha, motion and 142px
width literals superseded by the founder's current thinner uniform standard.

## Cause

The C26C gate mixed durable interaction invariants with exact predecessor
presentation spellings instead of accepting shared tokens whose values are
independently sealed by C27C.

## Permanent prevention

Migrate the full bounded predecessor token loop in one pass: preserve vertical
six-family order, 56px rows, finite 180ms motion, no modal/dimming, outside
dismissal, gestures and Back; resolve presentation metrics through the C27
shared tokens. Do not restore hardcoded predecessor styling.

## Resolution evidence

The complete C26C gate was inspected after the first rejection and this stale
contract registered before migration or retry.
