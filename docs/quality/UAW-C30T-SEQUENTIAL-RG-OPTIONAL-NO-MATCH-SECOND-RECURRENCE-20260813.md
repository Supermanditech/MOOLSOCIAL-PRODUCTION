# C30T sequential rg optional no-match second recurrence

Date: 2026-08-13

A third related diagnostic printed valid retired `mool-root-chat` and current `mool-global-chat` ownership, then returned exit 1 because an appended optional synonym search had no matches.

Permanent prevention: do not append optional `rg` queries. Use one command per required marker and stop once exact current and retired owners are established.
