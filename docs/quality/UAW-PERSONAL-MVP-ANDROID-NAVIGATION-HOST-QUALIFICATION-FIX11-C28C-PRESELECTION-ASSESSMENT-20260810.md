# C28C preselection — Android navigation host qualification

Classification: `mvp_required`.

Customer outcome: the bounded C28B viewport correction and unchanged C27
navigation pass two consecutive complete host cycles with one identical source
fingerprint before any successor APK authority opens.

## Reuse and scope

- Reuse the C27E 51-file/21-gate host qualification topology.
- Add the two C28B focused test files and C28B source gate.
- Reuse the same analyzer, formatting, protected Social/Buy seals, approved UI
  locks, delivery/scope gates and immutable evidence rules.
- Create no runtime owner, screen, route, copy, state, backend or build.

The ticket is test-only acceptance. It preserves installed r60.26 and keeps
build/install authority closed. Two consecutive cycles must each format all
listed owners, run complete mobile analysis, execute every listed test and
gate, and finish with the same SHA-256 source fingerprint. Estimated impact is
one day and remains inside the 60–75-day lock.
