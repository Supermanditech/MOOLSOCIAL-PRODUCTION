# UAW C16H exec-template backtick parser rejection — 2026-08-08

## Rejection

The direct aggregate retry placed PowerShell newline backticks inside a JavaScript raw template literal. The outer parser rejected the script before any shell command ran. No manifest, authorization, APK or device state changed.

## Prevention

The corrected calculation contains no inner backtick tokens and joins normalized inventory lines with `[Environment]::NewLine`. All required labeled outputs must be present before sealing.
