# C22C preselection assessment

C22C is MVP-required shared-runtime work in the existing global dock only. It reuses `MoolOutcomeDock`, `_MoolScrollableDockActions` and `_MoolMiddleDockAction`; no route, order, endpoint, meaning, screen, backend owner or state owner changes.

The six production middle actions already share `_MoolMiddleDockAction`. C22C replaces its 62 px/15-radius/9 px type/navy-fill language with the C22B 72 × 48/radius-24/icon-18/label-12 neutral capsule, and updates only the matching scroll extent/gap. Mool and Chat endpoints remain unchanged. Estimated impact is 0.5 day inside the delivery lock. Build/install remain closed.
