# C30S fresh semantic target disappeared before tap — 2026-08-13

The composer-recovery action first re-proved MoolSocial focus and captured a fresh hierarchy. It required exactly one `content-desc="Close composer"` node before deriving any coordinate. The hierarchy contained zero matches, so the step rejected before a tap.

No fallback coordinate, back press or blind recovery action occurred. The next permitted step is a read-only classification of the new current hierarchy. This fail-closed result reinforces that all device actions must be derived from the immediately current semantic state.
