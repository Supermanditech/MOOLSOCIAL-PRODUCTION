# C28D preselection — Android navigation OPPO qualification

Date: 2026-08-10

## Customer outcome and classification

C28D is `mvp_required` device acceptance work. One unique r60.27 successor may
be built only after the two C28C host cycles and fresh machine checks, installed
in place on connected OPPO CPH2375, density-normalized at the first native XML
gate, then exercised across all six accepted families and 18 local actions for
founder review.

## Qualified source and protected predecessor

C28C passed two consecutive complete cycles on fingerprint
`D113BFBD62BD0E183B3D289653C4F793CA9D2F8EFF8E30D53541DCB371408F18`.

The installed predecessor is r60.26 / `2026081026`, checksum
`A27200CEF659B3C1DC5F2C62ECBF6529B2F01B2036A41A0E1A7A87B186BDB1F4`.
It remains installed until one successor install is expressly authorized. Its
signer, package, first-install time and app data must be preserved.

## Reuse and duplicate disposition

Reuse the existing single-build wrapper, APK machine gate, in-place ADB upgrade
workflow, stable screenshot/XML capture, semantic-tap owner, C26H six-family
matrix topology and C28C evidence. Duplicate search found no density-normalized
Android XML bounds gate for the shared Mool/family/local rail. The only new
necessary owners are one C28D bounds gate, one versioned device contract and one
cumulative evidence package. No screen, route, action, copy, feature, backend or
provider owner is added.

## Smallest complete device scope

1. seal C28C and reconcile branch, HEAD, dirty tree and connected unlocked OPPO;
2. prove installed r60.26 package, version, checksum and first-install identity;
3. add and self-test a density-normalized UIAutomator XML gate before build;
4. seal a fresh source aggregate manifest and all prebuild checks;
5. open exactly one profile build authorization for r60.27 and use the existing
   wrapper once;
6. validate candidate package, version, signer and checksum, then consume build
   authority;
7. open exactly one in-place `adb install -r` authority after a fresh device
   gate; never uninstall, clear data, downgrade or use a second install;
8. prove local, live and pulled installed APK checksum identity and preserved
   first-install time;
9. launch the retained user state and apply the first native XML gate before
   any screenshot matrix: every Mool, family-root and local rail semantic must
   be at least 44 by 44 logical pixels at the live 320-dpi density;
10. if that material gate rejects, stop mutation and preserve truthful evidence;
11. if it passes, capture all six family roots, 18 local states, selected states,
    embedded switcher, swipe/tap/outside/Back, Mool, Chat, reduced-motion and
    content-reachability continuity;
12. leave the installed successor pending founder OPPO acceptance.

No runtime or screenbook mutation, second build/install, credentials,
messages/calls, funds, Production, commit, push, deploy or promotion is in
scope. Preselection opens no build or install authority.
