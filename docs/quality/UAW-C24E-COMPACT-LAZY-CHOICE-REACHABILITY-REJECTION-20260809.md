# C24E compact lazy-choice reachability rejection — 2026-08-09

At the 320x568, 140% text matrix point, the first focused test attempted to
measure later Doctor and Salon choices before their vertical lazy lists had
constructed them. The runtime rendered without an exception; the test's
immediate-presence assumption was invalid.

The corrected acceptance scrolls each keyed discovery list to the ordered
choice within a fixed bound before asserting the shared 44px minimum. The
vertical, one-tap product contract is unchanged. This failed invocation counts
as no qualification cycle.
