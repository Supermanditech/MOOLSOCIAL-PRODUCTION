# C30Y founder end-to-end authorization

Date: 2026-08-14
Ticket: `UAW-C30Y-R60-48-SUCCESSOR-AAB-PLAY-INTERNAL-OPPO-ACCEPTANCE`

The founder explicitly confirmed approval for the AAB flow end to end through
updating the existing OPPO app from Google Play, conditioned on converting and
implementing every audit finding before proceeding and on every regression
gate passing.

This records authorization for exactly:

1. candidate `1.0.0-r60.48` / version code `2026081348` / package
   `com.moolsocial.app`;
2. one successor AAB build through the visible founder launcher;
3. one upload and activation on Google Play Internal Testing only;
4. one in-place Play update of the existing app on OPPO CPH2375 `2b3e0f71`;
5. required post-update acceptance journeys.

The founder must personally enter the upload password, Firebase Android API
key and Google OAuth server client ID into the three hidden prompts. This record
does not provide or expose those values and does not authorize agent access to
them.

No other Play track, second build, ADB install, sideload, uninstall, data clear,
downgrade, backend/Hosting/provider deployment, email, quota submission,
credential access or funds are authorized.
