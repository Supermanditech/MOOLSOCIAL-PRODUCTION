# C17H founder-gallery inventory unsupported formatter rejection

Date: 2026-08-08

The second read-only gallery inventory used desktop `find -printf` syntax that the OPPO Android shell does not support. It returned one incomplete record and did not identify evidence. No device file was copied, moved, deleted or modified.

The retry contract is to combine Android-compatible path discovery with `toybox stat`, then select evidence only from complete path, timestamp and byte-size records.
