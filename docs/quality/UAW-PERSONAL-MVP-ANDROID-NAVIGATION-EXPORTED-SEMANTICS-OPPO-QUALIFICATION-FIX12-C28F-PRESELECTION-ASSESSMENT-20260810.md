# C28F preselection — exported-semantics OPPO qualification

Date: 2026-08-10

## Customer outcome and classification

C28F is `mvp_required` device acceptance work. One unique r60.28 successor may
be built only after the completed C28E identical-fingerprint host cycles and a
fresh read-only machine/device audit. It may then be installed in place on the
same OPPO CPH2375, density-normalized at the first native XML gate and exercised
across all six accepted families and 17 current local actions for founder
review.

## Qualified source and protected predecessor

C28E passed two consecutive complete cycles on fingerprint
`52E505B59809E920ED128E16CE8AD0DB0DDD7CB7CEAE0C3C8C5CD89CAA3B3989`.

The installed predecessor is rejected r60.27 / `2026081027`, checksum
`E4651AEADFD2A98A7617021B8DEF645BC5D428DD1593D882D278F3706FF6BD0C`.
It remains installed until one separately gated successor install is
authorized. Its package, signer, first-install time and app data must remain
preserved.

## Reuse and duplicate disposition

Reuse the existing single-build wrapper, APK machine gate, in-place ADB upgrade
workflow, stable screenshot/XML capture, semantic-tap owner and C28D matrix
topology. C28D's bounds/matrix contracts cannot be reused unchanged because
they require the founder-removed Social family-root and Buy Products cells.
The only new necessary owners are versioned C28F bounds/matrix contracts and
their gates/self-test plus one cumulative evidence package. No screen, route,
content, backend, provider or credential owner is added.

## Smallest complete device scope

1. close C28E and prove both immutable cycle hashes/fingerprint;
2. re-audit branch, HEAD, dirty tree, connected unlocked OPPO and installed
   r60.27 identity without launch, pull, force-stop or mutation;
3. self-test a density-normalized C28F UIAutomator XML gate that requires no
   Social family-root, no Buy Products/local Shop cell, and every visible Mool,
   required family-root and current local node at least 44 by 44 logical pixels;
4. seal a fresh source aggregate manifest and every required prebuild gate;
5. open exactly one profile build authorization for r60.28 and invoke the
   existing wrapper once;
6. validate candidate package, version, signer and checksum, then consume build
   authority;
7. open exactly one in-place `adb install -r` authority after a fresh immediate
   device/process audit; never uninstall, clear data, downgrade or install
   twice;
8. prove local, live and pulled installed APK checksum identity and preserved
   first-install time;
9. run the first stable native XML gate before any matrix interaction;
10. stop and preserve evidence if any target is below 44 by 44 or the current
    FSC01/FSC06 catalogue is wrong;
11. only after that pass, capture all six family roots, 17 local states,
    selected states, switcher, swipe/tap/outside/Back, Mool, Chat, IME and
    content-reachability continuity;
12. leave the installed successor pending founder review.

Preselection opens no build or install authority. No second build/install,
credentials, messages/calls, funds, Production, commit, push, deploy or
promotion is in scope.
