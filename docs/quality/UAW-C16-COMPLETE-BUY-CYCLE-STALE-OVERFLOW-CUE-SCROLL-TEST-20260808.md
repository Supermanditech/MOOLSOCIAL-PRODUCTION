# C16 complete Buy cycle stale lane-test rejection

The first attempted complete host cycle passed its 250-test universal/Social
and 58-test shared-family/vertical shards. The Buy shard then finished with
360 active passes, 20 protected-reference skips and one active failure:
`compact large-text navigation keeps fixed chrome and no overflow` still
required the removed overflow cue and horizontal scroll key.

That expectation belonged to the rejected predecessor. The durable case is
updated to prove the shared `MoolLocalNavigationRail`, a 304px four-action
cluster at 320px/140% text, absence of any descendant scroll/cue, direct
44px Orders reachability and preserved fixed header/Orders state. The two
required consecutive host cycles restart from zero after this correction.
