# C29X optional design-token search compound exit recurrence

- Date: 2026-08-11
- Ticket: `UAW-PERSONAL-MVP-SOCIAL-CHAT-GLOBAL-EDGE-AND-CONTRAST-C29X`
- Result: compound token diagnostic rejected

The required `MoolLocalNavigationTokens` declaration lookup succeeded, but the same invocation performed an optional navy-token lookup that returned zero matches and changed the compound exit code to one. This repeated the just-registered isolation defect. The required declaration path is re-established only through a separate required command; future token questions remain one invocation each with explicit match semantics. No source retry, test, build, install or device action followed the rejected diagnostic.
