# C10C home-delivery test retained top Back

The compact home-delivery control inventory still required `buy-back`. C10 removes top-level header Back in favour of system Back and stable global navigation. The stale key is removed from fitment coverage while exact Back behavior remains covered elsewhere.
