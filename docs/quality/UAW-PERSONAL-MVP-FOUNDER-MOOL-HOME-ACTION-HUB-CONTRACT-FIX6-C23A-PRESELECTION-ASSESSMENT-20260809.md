# C23A Mool Home action-hub contract preselection

C23A is the narrow contract-only response to the founder's r60.21 visual
rejection. It records the professional target before runtime mutation: remove
both persistent rails, retain one compact Mool Home launcher, and make the
existing Mool Home route the direct six-family/17-subaction hub.

Reuse search selects the existing `/app/mool` screen, six global action specs,
17 implemented subaction routes, Back behavior and Chat routing. No new screen,
route, backend owner, business state, provider integration or subaction is
necessary. One target tap from Home and at most two taps from a destination are
required without an intermediate family-expansion tap.

The installed r60.21 checksum identity remains preserved and visually rejected.
C23A authorizes reference, contract and gate work only; runtime, build, install,
backend, external service, credentials, messages/calls, funds and Production
mutations remain closed.
