# C30T poll-expiry dense evidence-command rejection — 2026-08-13

A dense PowerShell command combined release restoration, nested interpolation, multiple evidence paths and artifact checks. It exited immediately with no output, so no claimed result from that command is accepted.

Prevention: split release restoration from registrar, log-hash and preserved-AAB reads; keep each command simple enough to expose its exact failing boundary.
