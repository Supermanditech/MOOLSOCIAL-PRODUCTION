# C30A direct Create rail-sequence test migration rejection

The complete Social suite passed 153 checks except for two stale contract assertions. C29O selected direct Create and then searched for Feed in the intentionally hidden composer rail. Direct Create is posting-ready and contains the rail until composition closes. The test must make Create terminal in that sequence or close through the real composer owner. No product behavior is weakened.

Regression: `REG-20260811-1353-C30A-DIRECT-CREATE-RAIL-SEQUENCE-TEST-MIGRATION-REJECTION`.
