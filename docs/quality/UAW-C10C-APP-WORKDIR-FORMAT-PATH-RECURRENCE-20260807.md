# C10C app-workdir format path recurrence

A second format command ran from `apps/mobile` with `apps/mobile/...` operands, so all four paths were unresolved. Root-relative formatting and app-relative analysis now run separately.
