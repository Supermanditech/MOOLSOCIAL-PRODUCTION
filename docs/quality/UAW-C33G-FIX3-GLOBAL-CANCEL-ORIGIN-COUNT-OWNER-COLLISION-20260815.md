# UAW C33G FIX3 global cancel-origin count owner collision

The exact raw-source projection found three valid occurrences of the Social cancellation-origin fragment. Two belong to Create entry points; the third belongs to the separate message-author authentication path.

The gate now scopes one assertion to `_selectChoice` and one to `_openCreationGateway`. Whole-file global counts cannot be used when a fragment is legitimately shared by unrelated owners.
