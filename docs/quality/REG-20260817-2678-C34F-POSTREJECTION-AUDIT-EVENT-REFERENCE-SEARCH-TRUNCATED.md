# REG2678 — C34F post-rejection `$event` search was truncated

## Outcome

The first read-only diagnosis of `tmp/run-c30t-authoritative-flutter-manifest-audit.ps1` emitted all `$event` matching source lines and exceeded the retained output boundary. No line, scope or root-cause conclusion from that output is accepted.

## Prevention

Any successor diagnosis must first return only a scalar match count and bounded line-number batches, then read narrow explicit line ranges. C34F remains rejected at `0/0/0/0`; this record creates no successor, build, Play or device authority.
