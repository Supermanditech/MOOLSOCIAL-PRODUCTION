# C21 guessed placement state-file recurrence rejection

Date: 2026-08-08

The initial C21 inventory probed nonexistent
`config/personal-subaction-placement-regression-state.json`. This repeats the
registered C16 mistake. The correction reads the placement checker first and
uses only its literal
`config/mvp-personal-subaction-reachability-promotion-zone-regression.json`
contract.
