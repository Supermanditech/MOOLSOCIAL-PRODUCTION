# C21F preselection robustness and reuse assessment — 2026-08-08

C21F is MVP-required runtime/configuration and focused-test work on the existing `MoolDestinationNavigationV2`, `MoolDestinationFamilyWavePainter`, `MoolGlobalNavigationV2` and `_MoolMiddleDockAction` owners. It does not create a disclosure control or navigation owner; the selected main action remains the single 48 px-or-larger tap owner.

Minimum scope is default-expanded local choices; selected-main retap hides/restores only its own family; an unmistakable 18 px optical disclosure badge inside the selected 62×48 main-action target; truthful Hide/Show semantics; a restrained 1.25 px/24% connector; one selected sub-action grammar; 160–200 ms finite disclosure/connector/selection motion; immediate reduced motion; and preserved session-only state, Back, Mool, Chat, content and rail geometry/order/position/meaning.

Implementation disposition is reuse plus configuration with a one-day-or-less delivery impact and no topology growth. C21A–E, all family gates, protected UI/brand/copy/interaction gates, permanent memory and installed r60.19 identity are dependencies. Hidden gestures, extra taps, route/content changes, build/install, screenbook, backend/provider/business, credentials, messages/calls, funds, Production, commit/push/deploy/promotion remain excluded.
