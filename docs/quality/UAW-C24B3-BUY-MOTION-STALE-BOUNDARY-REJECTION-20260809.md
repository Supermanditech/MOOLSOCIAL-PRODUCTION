# C24B3 Buy motion stale boundary rejection — 2026-08-09

The six-file Buy motion batch passed 19 cases and failed two historical assertions. FIX13 searched the whole Wholesale screen for `Flexible packs` even though newer FIX14 correctly limits the absence rule to the compact header; the truthful body may use that copy. The theme-transition test still required the removed `mool-root-selected` rail key.

The corrections narrow the copy finder to `buy-contextual-glass-header` and replace the retired rail assertion with the current `mool-home-launcher`. No Buy business copy, route or state changes.
