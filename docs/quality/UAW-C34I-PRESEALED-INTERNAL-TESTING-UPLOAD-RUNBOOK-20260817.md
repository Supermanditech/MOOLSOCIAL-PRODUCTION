# C34I pre-sealed Internal Testing and OPPO runbook

Candidate:
`UAW-C34I-R60-73-AUTHENTICATION-PRIVACY-SAFE-PLAY-OPPO-ACCEPTANCE`,
`1.0.0-r60.73` / `2026081373`.

## Permanent boundaries

- Google Play Internal Testing only.
- One AAB, one upload/activation and one Play in-place OPPO update only after
  their exact machine gates.
- No ADB install, sideload, uninstall, data clear or downgrade.
- The founder alone enters hidden build inputs and handles every account-
  capable provider tile, account chooser, account selection, private
  identifier, email, phone, OTP, credential or private link.
- Codex resumes only after the founder confirms every private system surface
  is closed, then reselects the exact live CPH2375 mirror and discards all
  earlier screenshots, coordinates and element indices.
- Any private identifier, chooser, credential, private link or unplanned
  provider surface visible to Codex rejects the candidate. No waiver applies.

## Source qualification

Before the source seal, create every ticket, state, aggregate, actor-policy,
gate, recovery, cycle, launcher and evidence owner. Generate the focused and
whole-source manifests only after all owners are final. Run both fresh complete
cycles while detailed and aggregate state remain `0/2`. Do not persist cycle 1
as `1/2`. Verify both retained summaries independently, then atomically persist
`2/2` and both evidence paths. Expose founder-prompt authority only after
dual-host replay.

After the source seal, repository discovery commands are prohibited. No
post-seal source, registry, ticket, runbook or gate mutation is allowed. A new
finding rejects C34I and requires a separately sealed successor.

## Founder-input AAB

The founder launches only the retained visible PowerShell 7 command and enters
the three hidden values. Codex never launches the console, inspects its terminal
or reads a secret-adjacent parent object. The launcher must pass `preprompt`,
validate inputs locally, project
`founder_inputs_validated_single_aab_build_required`, call the generic wrapper
once and clean all transients before retaining a bounded visible result.

## Play upload and activation

After `postbuild`, prequalify the visible Upload control before consuming the
one upload authority. Do not enumerate open tabs, history, query, fragment,
account, tester, private-link, cookie, storage or session data. Open only the
known MoolSocial Internal Testing route and retain query-free allowlisted
host/path plus non-private release fields. Activate no other track.

## Play in-place OPPO update

After `postupload`, the founder performs the normal Play in-place update on
OPPO `2b3e0f71` / `CPH2375`. No ADB mutation is allowed. Codex may retain only
sanitized package, version, installer, activity, process, fatal/ANR and
non-private MoolSocial screen facts.

## Device journeys

Codex may open public Guest Feed and the protected Social Create gateway, but
stops before every provider tap. The founder performs Google, Mobile OTP and
passwordless email actions only when separately authorized, closes private
surfaces and reports a sanitized result. Codex then reselects the exact device
window and verifies only sanitized post-authentication MoolSocial state.

Device acceptance remains `0` until all six C33G blockers and every mandatory
whole-app journey have candidate-specific retained evidence. Build, upload and
install success do not imply device acceptance or production readiness.
