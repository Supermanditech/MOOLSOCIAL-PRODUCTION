# UAW C18A Screen01 v3 three-state golden-divergence rejection — 2026-08-08

## Result

C18A host analysis and the focused brand-motion, progressive launch,
interruption and Screen01–03 fitment suites passed. The immutable Screen01 v3
golden suite then rejected every protected state:

- normal-motion midpoint: `7.06%`, `18,299` pixels different;
- slow handoff: `9.45%`, `24,494` pixels different; and
- recovery: `10.37%`, `26,874` pixels different.

The failure feedback generated by Flutter remains preserved under
`apps/mobile/test/failures`. No golden was updated.

## Disposition

The current direct Screen01 source and test match the exact R50 source
manifest, but these three failures prove that the active presentation is not
pixel-equivalent to immutable v3. R50 authority accepted the progressive
full-wordmark launch treatment; it cannot automatically be stretched into an
acceptance of unrelated slow/recovery drift or used to relabel all current
pixels as a new immutable reference.

C18 reference mutation is rejected. No v4 package, manifest status change,
source restoration, runtime correction, build, install, uninstall, data clear
or downgrade is authorized by this result. The exact divergence must be
separated into founder-accepted R50 progressive-launch pixels and any
out-of-scope v3 slow/recovery drift before a successor ticket can be selected.

The installed OPPO replay also remains pending because the secure keyguard is
showing and device screenshots cannot be captured without founder unlock. No
credential was requested, accessed or bypassed.
