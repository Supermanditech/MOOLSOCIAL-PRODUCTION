# C22E legacy C11 connector-contract rejection — 2026-08-08

The six legacy C11 cases expected `selectedMainActionAnchor.dy == selectedMainRect.top - globalStack.top` (0), while the authorized C22E painter terminates at `bridgeHeight - 1` (59) in its separate overlay. The same test later expected the superseded selected-local directional wave. REG-20260808-541 migrates C11 to the stable reverse-U full-family span while retaining one-tap selection and semantics proof.
