# C10C Buy category sheet retained retired dock inset

The category sheet still reserved the former 54-pixel Buy dock height after the shared global navigation surface became 48 pixels. Its surface therefore ended 6 pixels above the global dock. The sheet now consumes the shared compact navigation token, and the full Buy suite enforces a seam within one pixel.
