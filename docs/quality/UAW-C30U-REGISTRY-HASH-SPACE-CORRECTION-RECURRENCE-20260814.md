# C30U registry hash-space correction recurrence

The first correction still retained one grouping space, and the progress text
repeated the malformed display. The exact current checksum is now copied from
the machine value and validated as 64 hexadecimal characters.

No release mutation occurred.
