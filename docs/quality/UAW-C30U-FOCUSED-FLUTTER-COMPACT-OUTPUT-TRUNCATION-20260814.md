# C30U focused Flutter compact-output truncation

Compact reporter progress for the Social batch expanded and truncated the
failure detail. The stream is rejected.

Diagnosis uses one named expanded test; qualification uses captured JSON and a
counted summary. No release mutation occurred.
