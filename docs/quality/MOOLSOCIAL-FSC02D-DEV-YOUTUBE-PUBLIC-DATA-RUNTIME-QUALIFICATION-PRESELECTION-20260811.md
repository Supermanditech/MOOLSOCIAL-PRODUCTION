# FSC02D Dev YouTube public-data runtime qualification preselection

- Ticket: `MOOLSOCIAL-FSC02D-DEV-YOUTUBE-PUBLIC-DATA-RUNTIME-QUALIFICATION`
- Classification: `mvp_supporting`
- Actor: an authenticated Personal user on founder-authorized OPPO serial `2b3e0f71`.
- Customer outcome: real eligible YouTube public Shorts and Videos load through the accepted MoolSocial Dev provider with Firebase Auth and genuine Play Integrity App Check, while every owner, upload, live and analytics capability remains disabled.
- Delivery impact: one existing-runtime qualification and profile-device slice inside the robust-MVP delivery lock.

## Reuse and duplicate assessment

The repository already owns the complete bounded system needed for this outcome: the `moolsocial-dev-503018` provider, fail-closed `PublicDataReview` profile, Firebase callable client, positive public/processed/embeddable/India admission, official player, Auth/App Check headers, loading/error/retry states, deployment controls and previous real OPPO evidence. The durable handoff records that the founder accepted persistent public viewing on 25 July 2026, revision `youtubeprovider-00024-dol` served real Videos and Shorts, genuine Play Integrity was accepted and unauthenticated App Check requests were rejected.

No new screen, route, API client, provider, backend service or content source is justified. The provider HTTPS URI is configuration, not a credential. It must be derived from the exact reviewed deployed function and supplied only to the authorized Dev profile build.

## Smallest complete scope

1. Confirm the current Cloud/Firebase account is authorized for the exact Dev project before using it.
2. Verify the live function inventory and `PublicDataReview` flags read-only, with every owner/write capability false.
3. Qualify the existing provider URI, Firebase Android identity, Auth provider state, Play Integrity registration, signing fingerprint and App Check rejection boundary without reading or copying any secret value.
4. Prove `capabilities.publicData=true` and real eligible public catalogue behavior through the existing client/provider and focused backend/Flutter tests.
5. Redeploy the exact reviewed Dev provider only if read-only evidence proves the existing revision stale or unusable. Any redeploy still requires the repository's exact `KEEP_YOUTUBE_PUBLIC_DATA_REVIEW_LIVE_IN_DEV` confirmation lock.
6. After runtime qualification, register a fresh checksum-unique profile APK candidate, pass host gates, install without clearing the protected OPPO data and test the complete Shorts journey.

## Security correction

The existing security prerequisite script reads the restricted API key value and Secret Manager values in-process to compare hashes. That behavior conflicts with the founder's standing no-credential-access boundary. FSC02D must not execute that path. Production-grade qualification will instead rely on non-secret resource identity, key restriction metadata, exactly one enabled secret version, exact function secret bindings and IAM, no debug tokens, genuine App Check, and successful guarded end-to-end public-data requests. Secret values, access tokens, OAuth secrets and encryption keys must never be copied or emitted as evidence.

## Explicit holds

This ticket does not enable owner connect/actions, uploads, Live, Analytics, broader OAuth scopes, fake content, Staging, Production, release, submission or promotion. It does not authorize messages, calls, fund movement, commit or push. Installed r60.28 and all C28D/C28F evidence remain protected until a separately registered, host-qualified and checksum-unique successor is ready. No uninstall, data clear or downgrade is permitted.

Founder review begins only after the real Dev runtime and checksum-matched OPPO journey pass.
