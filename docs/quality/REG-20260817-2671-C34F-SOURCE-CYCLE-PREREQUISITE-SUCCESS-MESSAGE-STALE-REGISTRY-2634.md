# C34F source-cycle prerequisite success message stale registry-2634

Date: 2026-08-17 IST

Status: registered pre-seal; exact final registry literal required

Before the prerequisite-only runner was executed, exact readback proved its
success message still said `registry-2634` even though `manifestRelative`, the
candidate state and the aggregate target the later final registry generation.
No false pass was emitted and no test, build, browser, Play or OPPO action ran.

Bind that success message to the exact final C34F registry number, assert the
literal from the candidate gate, parse both PowerShell hosts, rebind the
registry and ticket hashes and replay source composition before the official
seal.
