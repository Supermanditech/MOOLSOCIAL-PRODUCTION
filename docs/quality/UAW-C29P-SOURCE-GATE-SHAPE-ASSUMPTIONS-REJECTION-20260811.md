# C29P source-gate shape assumptions rejection

Early gate passes guessed equivalent authentication copy and identifiers, asserted a formatted message as one line, and looked for Reply fail-closed wiring in the leaf instead of the consumer callback owner. The gate now uses exact stable literals and actual ownership boundaries.
