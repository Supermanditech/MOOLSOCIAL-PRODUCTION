# Superadmin MVP exact launch-participant provisioning and control portfolio

Date: 7 August 2026
Parent: `SUPERADMIN-MVP-EXACT-LAUNCH-PARTICIPANT-PROVISIONING-AND-CONTROL-END-TO-END-JOURNEY`
State: founder-authorized sequential execution, subject to every independent gate

## Customer and operator outcome

An authenticated Personal user can reopen only a server-owned workspace
membership identified by a stable ID. Each exact launch participant receives
only separately verified capability, geography, category and validity. Each
exact reviewer or operator can decide, canary, monitor, pause or roll back only
the scope its role owns. Drafts remain non-live and budget-neutral; denied,
held, expired, suspended or revoked state remains truthful and recoverable.

## Classification

The portfolio contains 45 exact acceptance children: 43 `mvp_required`, two
`mvp_supporting`, and zero `beyond_mvp`. It is required because authoritative
workspace membership, capability publication and governed launch are blocking
the workspace and launch journeys already inside the founder-defined MVP.

## Shared implementation topology

- Reuse the separate `apps/admin` Next.js Superadmin, Screen 156 provisioning
  owner, existing 42 operating cases and the registered 29-profile list.
- Reuse the backend supply-participant contract's stable IDs, tenant checks,
  versioning, command receipts, idempotency, state and audit patterns.
- Add one general account-to-workspace membership domain owner because the
  supply contract intentionally owns seller capabilities and cannot truthfully
  establish a Personal account's permitted workspace list.
- Express exact profile/capability differences as configuration or thin policy
  adapters. Exact acceptance children do not create 45 screens, routes,
  services or builds.
- Keep creator children SAM-R26 and SAM-R27 held until the recorded Social /
  YouTube dependency closes.

## Execution order

The exact child list, actor, capability, outcome, classification, dependency,
lane and implementation disposition is the machine manifest at
`config/superadmin-mvp-exact-launch-participant-provisioning-control-portfolio.json`.
Only one child may be active. The first independently executable child is
`SAM-R01-SERVER-STABLE-WORKSPACE-MEMBERSHIP-READ-CONTRACT`.

## Smallest complete SAM-R01 scope

Create one pure server-domain contract that validates and projects an
authenticated account's workspace memberships. It accepts only stable account,
workspace and membership IDs; enforces account ownership; distinguishes
active, pending, revoked and expired records; eliminates exact duplicate
records deterministically; rejects conflicting duplicates; sorts output
deterministically; and returns immutable safe projections without capability
inference. Add exhaustive local tests.

SAM-R01 adds no UI, screen, route, persistence store, Firebase function,
credential, production data, live role, workspace creation, capability grant,
provider action, APK, build, install, deploy or promotion. Persistence,
authenticated query, Admin command and Flutter integration remain separate
children.

## Permanent release and safety boundary

Production access remains denied by default. No child authorizes credentials,
live provider messages or calls, money movement, raw audience export,
Production reads/writes, commit, push, deployment or promotion. UI work retains
its independent accepted-reference workflow. Backend/environment work remains
local-emulator-first and cannot infer a live provider or reviewer result.
