# Screen 03 — Login / Account Handoff — native production v4

Production status: **Founder Accepted — immutable**

Screen03 v4 preserves the founder-accepted customer-visible presentation,
routes, copy, provider assets, behavior tests, fitment tests and goldens
byte-for-byte. It adds no screen, route, action, feature or runtime change.

This version exists only to reconcile the non-visual production test lock after
the accepted C30W cold-start repair moved release configuration into one shared
contract and replaced a pre-`runApp` abort with a safe customer first frame.
The same test continues to retain the already accepted profile candidate and
startup provenance assertions from v3.

Screen03 v1, v2 and v3 remain immutable history. This v4 directory and every
hash recorded for it are immutable. A future replacement requires another
founder-directed version and acceptance record.
